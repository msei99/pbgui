---
# Update pbgui on localhost
# Steps:
# 1. Clone/update pbgui repository
# 2. If updated, install requirements, sync systemd units and restart PBCluster/PBCoinData

- hosts: localhost
  gather_facts: "{{ debug }}"

  vars:
    pbgdir: "{{ pbgdir }}"

  tasks:
    - name: display facts
      debug:
        var: ansible_facts
      tags: debug,never

    - name: Get current branch
      command: git rev-parse --abbrev-ref HEAD
      args:
        chdir: "{{ pbgdir }}"
      register: current_branch
      changed_when: false
      when: pbgdir != ""

    - name: clone pbgui repository
      git:
        repo: https://github.com/msei99/pbgui.git
        dest: "{{ pbgdir }}"
        version: "{{ current_branch.stdout }}"
        update: yes
        force: yes
      when: pbgdir != ""
      register: pbgui_repo
      notify: restart pbgui

    - name: Check required PBGui systemd units
      shell: |
        target_user={{ user | default('', true) | quote }}
        target_home=""
        if [ -n "$target_user" ]; then
          target_home="$(getent passwd "$target_user" | cut -d: -f6)"
        fi
        unit_dir="${target_home:-$HOME}/.config/systemd/user"
        for unit in pbgui-api.service pbgui-pbcluster.service; do
          if [ ! -f "$unit_dir/$unit" ]; then
            echo "missing=$unit"
          fi
        done
      args:
        executable: /bin/bash
      when: pbgdir != ""
      register: required_systemd_units
      changed_when: "'missing=' in required_systemd_units.stdout"
      notify: restart pbgui

    - name: print pbgui_repo
      debug:
        var: pbgui_repo
      tags: debug,never

  handlers:
    - name: Install pbgui requirements
      pip:
        requirements: "{{ pbgdir }}/requirements.txt"
        extra_args: --upgrade pip
      listen: "restart pbgui"

    - name: Sync PBGui systemd user services
      command:
        argv:
          - /bin/bash
          - "{{ pbgdir }}/setup/setup_systemd.sh"
          - --pbgui-dir
          - "{{ pbgdir }}"
          - --python
          - "{{ ansible_playbook_python }}"
          - --enable
          - api,pbcluster
          - --no-disable-excluded
          - --no-start
      register: systemd_setup_result
      changed_when: systemd_setup_result.rc == 0
      listen: "restart pbgui"

    - name: Restart PBCluster and PBCoinData
      script: setup/vps_service_control.sh restart PBCluster PBCoinData
      environment:
        PBGUI_DIR: "{{ pbgdir }}"
        PBGUI_PYTHON: "{{ ansible_playbook_python }}"
      listen: "restart pbgui"
