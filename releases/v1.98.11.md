# v1.98.11

## Fixed

- Fixed VPS Manager PB8 instance links opening the PB7 log location and showing an empty Host Log Viewer instead of the selected bot's PB8 log.
- Fixed PB8 CPU, memory, and swap metrics remaining at zero with empty history because the monitor agent resolved the virtualenv Python symlink before locating the PB8 CLI; PB8 history links now use the runtime-qualified key and valid zero-swap samples are retained.
