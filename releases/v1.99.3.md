# v1.99.3

## Added

- Added a universal safe UI-control inventory for PBGui AI so explicit chat requests can activate visible non-sensitive buttons and links or set visible non-sensitive fields without feature-specific actions.

## Changed

- Added a local fast path for unambiguous reversible UI commands such as showing or closing a log window, recording them in conversation history without waiting for an AI provider.
