# v1.93

## Changed

- Remove the completed CMC API pool project from the open product roadmap.
- Remove completed credential-store, KuCoin assessment, cluster-sync, retention, INI-modernization, logging-migration, versioned config-archive, VPS monitor-agent, Bitget market-data, and OKX market-data plans.
- Add complete English and German DB Tools guides and map every registered page to a shared help topic.
- Open Strategy Explorer's existing bilingual documentation through the shared Help overlay.
- Document the state-based Cluster Sync retention gates and manual peer-firewall requirements accurately.
- Document the shipped Bitget REST, auto-refresh, status, and distributed Best 1m workflows in the English and German Market Data guides.

## Fixed

- Preserve archived Backtest defaults, protect foreign archive content, and resolve Optimize config import collisions explicitly.
- Harden archive migration, generated-file writes, copy staging, Git pull safety, cleanup verification, manifest fallback, and concurrent imports against malformed data, symlinks, dirty worktrees, and races.
- Keep the PB7 upstream shortcut on PBGui's exact pinned PB7 commit instead of targeting PB8 master.
- Open new VPS Manager task logs empty, then load the new run from its beginning after the backend confirms log rotation.
- Show exact pinned PB7 checkouts as current instead of comparing them with PB8 upstream master.
- Report precise apply timing after Welcome setup saves instead of claiming that PBRun startup settings apply immediately.
- Honor configured log purge size and backup retention, prune excess generations, redact failures, and keep VPS Manager API logs in `PBGui.log`.
- Harden systemd deployment, update stability checks, migration state preservation, PB7 path handling, and VPS purge cleanup.
- Harden VPS Manager and Monitor cache health, compatibility fields, rendered host/bot actions, cookie logout, and WebSocket session-expiry handling.
- Make VPS monitoring cache-only with strict schema validation, 15-second live and 30-second collector health, deterministic task shutdown, agent-only package provenance, and cookie-only browser authentication.
- Prefer the freshest monitor-agent metrics cache during rolling upgrades so stale canonical data cannot shadow an active legacy stream.
- Keep Bitget minute heatmaps on the source index and limit REST retries to transient transport, server, rate-limit, and JSON failures.
- Keep OKX backfills fail-closed on exhausted REST chunks, preserve existing candles on failed latest refreshes, bound cancellation work, restrict retries to transient failures, and use the OKX refresh interval for heatmap freshness.
