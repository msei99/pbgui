# v1.79

Released: 21-05-2026

- Fixed the VPS manager local master sidebar update indicators so PBGui and PB7 turn orange when updates are available.
- Reduced the remaining legacy PBv7 Streamlit Run, Backtest, Optimize, and Balance Calculator pages to minimal FastAPI relay/redirect fallbacks instead of keeping duplicate editor and calculator flows alive.
- Routed legacy Streamlit Run, Backtest, Results, and Pareto handoffs through FastAPI draft/queue endpoints so the active PBv7 run, backtest, optimize, and live-vs-backtest flows now converge on the API/web stack.
- Removed the obsolete legacy `RunV7.py`, `BacktestV7.py`, `OptimizeV7.py`, `VPSManager.py`, and unused optimize helper modules `MultiBounds.py`, `OptimizeConfig.py`, `OptimizeScore.py`, `NeatGrid.py`, `RecursiveGrid.py`, and `Clock.py`.
- Removed the old `Config.BalanceCalculator` class and obsolete `V7Instances` session initialization that were only needed by the retired Streamlit PBv7 editor flow.
- Renamed the FastAPI backtest/optimize service labels consistently across service status, grouped logging, and help text, and refreshed user-facing docs so they no longer refer to removed legacy page names.
