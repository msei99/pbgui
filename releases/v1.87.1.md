# v1.87.1

- Fixed Config Archive pulls after compacted/force-updated remote history by backing up the previous local HEAD before resetting recoverable clones to the remote branch, including clones left with PBGui's local legacy-layout migration diff.
- Added live progress output for Config Archive Pull and Pull All operations so long Git updates show elapsed time and streamed command output while running.
- Forced Config Archive Git command output to English so localized server environments do not show non-English text in the GUI.
