# v1.90.1

- Fixed VPS Monitor SSH status flapping when a reachable host has a stale or missing monitor-agent metrics stream; stale metrics now restart the stream without forcing the SSH row red, and the Services tab now shows the dedicated PBMonitorAgent service.
- Fixed Cluster V7 saves resolving a target VPS to a newly created disabled duplicate node when local host-node mappings were stale; existing reachable nodes are now preferred by hostname/pbname.
