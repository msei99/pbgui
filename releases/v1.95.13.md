# v1.95.13

## Fixed

- PBGui runtime roles are again limited to `master` and `slave`; new VPS installations use `slave`, updates automatically migrate the accidental `vps` runtime role, and the VPS Manager maps legacy telemetry to `slave` while retaining `vps` only for internal Cluster identities.
