# v1.98.23

## Fixed

- PB8 Pareto refinement presets now start from the selected candidate via the immutable queue snapshot instead of launching fresh and potentially failing to reproduce the known result inside narrowed bounds.
