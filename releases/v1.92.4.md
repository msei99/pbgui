# v1.92.4

- Fixed VPS deploy progress remaining at `starting` when a run-specific log header and its persisted metadata were written on adjacent seconds.
- Fixed PBGui updates silently remaining on an old commit when a host repository had a branch-restricted Git fetch refspec.
