# v1.95.15

## Added

- Added a structured implementation plan for daily OHLCV finalization, targeted repair of known Bybit gaps, a central daily-checksum SQLite catalog, and GitHub-distributed read-only optimizer comparison.

## Fixed

- Prevented delayed Backtest config loads from leaving the editor sidebar over the Results panel and hiding actions such as Compare.
- Added a PBv8 Run update warning only when PBRun is blocked by an invalid PB8 runtime, with affected hosts and a link to VPS Manager.
- Kept an existing Run target visible when one master has stale or negative host capability data instead of falsely displaying the instance as disabled.
- Displayed canonical PB7 forced-mode values such as `graceful_stop` in the matching Run editor dropdown instead of leaving it blank.
- Prevented partial Bybit pagination from overwriting complete OHLCV days, verified every refreshed closed day has 1,440 contiguous minutes, retried incomplete days on later hourly cycles, and replaced stale source-index coverage on overwrite.
