# Unreleased

- Add configurable PBCluster history retention with fail-safe report-only default, signed cluster-wide policy, deterministic current/previous checkpoints, migration-seal validation, full-replay-equivalent checkpoint-tail reducers, all-replica signed commit proofs, stale-node and join rebootstrap, crash-resumable operation pruning, and independently gated two-pass reachable-blob cleanup.
- Add Cluster Retention controls and bounded per-node reports without exposing browser session tokens.
