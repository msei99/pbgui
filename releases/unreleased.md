# Unreleased

- Add the FastAPI Pareto Explorer `Create PBv7 Optimize Preset from this Config` expander with the Streamlit preset controls, live scoring/limits/bounds previews, and create flow that reuses the existing Optimize config save and Optimize draft-open endpoints.
- Fix the Pareto Explorer preset generator `Near-edge expansion (%)` slider so dragging it fully left keeps and submits `0%` instead of falling back to the default `25%`.
- Add a streamlit-free Pareto optimize preset generator plus PB7 bridge metric-set helper, keeping PB7 metric metadata access centralized in `api.pb7_bridge` and documenting current PB7 integration touchpoints for future compatibility checks.
- Keep the Deep Intelligence `Top N Parameters` slider stable by clamping its own 10–40 range instead of reusing the general max-config normalization, and remove the extra lower parameter-bound list so the tab focuses on the aggregate charts.
- Hide the whole `selected-config-detail` section (not just the inner grid) when the Parameters tab is active, so the charts can fill the full width on first load.
- Move the `selected-config-detail` section after `stage-deep-intelligence` in the DOM so it renders at the bottom for Scenarios and Evolution tabs (Config Preview below the chart), while Parameters and Correlations tabs hide it completely.
- Wire the Deep Intelligence Evolution tab controls (metric, weighted/BTC toggles, window slider) to auto-refresh on change instead of requiring manual button press, matching the live-updating behavior of the Parameters tab. Remove the now-redundant Refresh button and placeholder text.
- Fix Pareto Explorer config detail lookup to use the full loader (all loaded configs) instead of the visible-range slice, so clicking a timeline star after changing the Display Range no longer returns "Config not found".
- Fix Deep Intelligence Evolution reloads and metric switches to use the same visible-range slice loader as the rest of the UI, so a `Display Range` of `500` consistently renders 500 configs instead of all loaded configs or a sparse `config_index` subset.
- Add a `Show all configs` toggle to the Deep Intelligence Evolution toolbar, defaulting to off so the timeline shows only visible pareto configs until explicitly switched to all visible configs.
- Add a `Hide liquidation outliers` toggle to the Deep Intelligence Evolution toolbar, defaulting to on so extreme `-1`-style liquidation points can be removed from the chart scale without losing the ability to inspect them when needed.
- Clarify Deep Intelligence Correlations `Diverse Styles` by selecting configs with a safe round-robin over the available style categories and showing the actual number of style categories in the summary.
- Keep Deep Intelligence Plotly panels, especially `Scenarios`, on a stable minimum chart height and force a delayed resize on window/sidebar width changes so scenario boxplots do not collapse into a thin strip after resizing the page.
- Make the Deep Intelligence `Scenarios` metric selector auto-refresh immediately, remove the redundant `Refresh` button and empty placeholder text, and add more separation between the chart and scenario-stat cards.
- Reorder Deep Intelligence `Scenarios` statistics into per-scenario columns aligned to chart label order (e.g. `Base` block left, `Doge` block right) so values are grouped predictably instead of interleaved across mixed cards.
- Hide the shared selected-config detail panel on the Deep Intelligence `Scenarios` tab, because scenario boxplots/stats are aggregate over the visible config set and should not imply a per-config selection context there.
- Clarify Deep Intelligence `Scenarios` copy to explicitly state that scenario boxplots/statistics are aggregated over the visible config set.
- Keep Deep Intelligence `Correlations` radar sizing stable by pinning the chart footprint and polar/legend layout so the radar no longer grows/shrinks with selected config count or overflows outside the card.
- Make Deep Intelligence `Parameters` charts render at full width without an obvious narrow-first flash by hiding each Plotly chart until its first resize pass completes.
- Enrich Deep Intelligence `Parameters` heatmap hover with nearest-bound context (`Upper`/`Lower`, distance %, observed value, bound value) so the parameter influence view also shows how close each parameter currently sits to its optimize limit.
- Make Deep Intelligence `Parameters` render both charts at their final backend-provided height up front, preventing temporary overlap/cropping and preserving the gap between the heatmap and near-bounds chart while resizing or changing `Top N Parameters`.
- Limit Deep Intelligence `Parameters` to the actual near-bound parameter count instead of forcing extra rows from the requested slider value, and keep row height stable by sizing both charts from the real displayed parameter count.
- Stop Deep Intelligence `Parameters` charts from flashing visible, disappearing, and then reappearing on tab entry by updating existing Plotly charts in place instead of re-hiding them during every refresh.
- Fix the Deep Intelligence `Top N Parameters` slider fill after clamping to the real near-bound count, so the blue range track immediately matches the displayed capped value instead of briefly showing the old larger selection.
- Prevent the Deep Intelligence `Parameters` charts from appearing at a stale hidden-tab width by resizing existing Plotly nodes immediately when the tab becomes visible and rendering updates with the measured container width up front.
- Render server-bundled Deep Intelligence payloads only after applying the active Deep Intelligence tab UI, preventing Plotly from measuring Parameters charts while their panel is still hidden or in a pre-tab layout state.
- Stop pre-rendering Deep Intelligence into hidden stages during the first page bootstrap/load path; the active Deep Intelligence tab now loads only once its stage is visible, eliminating the first-open hidden-width Parameters render.
- Align Deep Intelligence `Scenarios` statistic cards to the actual Plotly scenario columns by using fixed per-scenario grid columns and the chart's left/right plot margins, so `Base`/`Bybit`/`Binance`/`Multi` cards stay under their matching boxplots across window sizes instead of auto-flowing into unrelated positions.
- Restore real vertical spacing between the Deep Intelligence tab shell card and the active analysis card by turning `stage-deep-intelligence` into an explicit stacked flex container with a gap, and keep scenario statistics out of the generic 3-column metric grid so four-scenario layouts can span all four chart-aligned columns.
- Remove the leftover generic `metric-grid` class from Deep Intelligence `Scenarios` statistics so the scenario-aligned grid can actually use the full available width instead of being constrained into the legacy 3-column metric layout.
- Restore the shared selected-config detail panel when switching back from Deep Intelligence to `Overview` or `Explorer`; the prior tab-specific hide/show logic left the common config/metrics section stuck hidden until a later Deep Intelligence tab made it visible again.
- Harden the shared selected-config detail panel visibility by restoring explicit `block`/`grid` display states for `Overview`/`Explorer` and giving the panel its own stacking context, preventing it from remaining hidden or seeming to sit behind Plotly charts after Deep Intelligence stage switches.
- Add chart captions above the two Parameters tab charts: "Parameter Influence Heatmap — correlation between most variable parameters and performance metrics" and "Parameters Near Bounds — parameters within 10% of their optimization bounds (red < 5%, orange 5–10%)".
- Make the Evolution rolling-average line use all visible configs even in pareto-only mode, and size its smoothing window from the visible slice instead of the full result, so the blue line stays visible for paretos and no longer starts with a misleading 10,000-config-scale dip.
- Start the Evolution rolling-average line only after a full smoothing window is available, so early outlier configs no longer create an artificial downward spike at the left edge of the chart.
- Compute the Evolution rolling-average from the same dataset that is actually being displayed (paretos when `Show all configs` is off, all visible configs when it is on), so the blue line no longer shows discontinuous jumps caused by mixing full-slice smoothing with pareto-only points.
- Resize the Deep Intelligence Parameters charts once more after render so the first load in that tab uses the full available width even when the hidden config-detail panel was visible in the prior view.
- Match the Streamlit Parameters Intelligence controls more closely by replacing the FastAPI `Top N Parameters` dropdown plus `Refresh` button with a slider that updates the tab directly.
- Apply the Pareto Explorer dark Plotly theme consistently across Deep Intelligence charts so heatmaps, bounds charts, scenario plots, evolution timelines, and correlation radars no longer render with white backgrounds.
- Include the actual config number in Pareto Explorer radar `Best Match` labels and hover text, including the `Risk Profile` quick view.
- Rebuild Pareto Explorer `Risk Profile` radar traces as non-interactive fill layers plus clickable line/marker layers so overlapping radar areas no longer steal mouse hit-testing from earlier configs.
- Remove a duplicated script/footer tail from the FastAPI Pareto Explorer HTML so shared navigation code only initializes once and the nav restart button behaves reliably on that page.
- Fix Pareto Explorer radar selection by attaching each radar trace to its config index and teaching the shared Plotly click handler to resolve config indices from overlapping radar points and trace metadata, so every compared config remains selectable.
- Rework Pareto Explorer radar charts to match the Streamlit presentation more closely by restoring dotted guide circles, axis spokes, padded axis labels, and a dedicated `Risk Profile` quick view alongside `Top Comparison`.
- Restore a compact Pareto Explorer projection color-scale label as a small centered caption above the legend, so side-by-side charts keep the metric name visible without reintroducing wide empty margins or text overlapping the scale.
- Hide Radar Chart playground controls that do not affect radar output (`Show all configs`, `Use Weighted Metrics`, `Use BTC instead of USD`, and `Color by`) so the settings panel only shows options that actually matter in that mode.
- Simplify Pareto Explorer projection colorbars by removing the extra legend-side metric label and tightening the right margin, so side-by-side charts no longer waste space or merge label text into the color scale.
- Stop Pareto Explorer projection charts from emitting a Plotly title when the projection title prefix is blank, so side-by-side projection labels only appear in the dedicated heading above each chart instead of still rendering inside the plot area.
- Remove the duplicate Pareto Explorer projection labels from inside the Plotly charts, move `XY`/`XZ`/`YZ projection` into real headings above each side-by-side panel, and stop the backend from injecting extra projection titles into those plots.
- Fix Pareto Explorer side-by-side projection panels by moving `XY`/`XZ`/`YZ projection` labels into real DOM headings above each chart, removing the stray placeholder text inside/beside the plots, and restoring the normal Plotly projection layout so the color scale renders again.
- Give Pareto Explorer projection panels their own compact frontend Plotly layout: move `XY`/`XZ`/`YZ projection` labels into top chart titles and tighten small-panel colorbar spacing so side-by-side mode stops wasting right-side space or drawing the metric label inside the legend.
- Push the Pareto Explorer `3D Projections` vertical color-scale label farther out into a wider right margin so it no longer overlaps the legend tick numbers.
- Move the Pareto Explorer `3D Projections` vertical color-scale label to the outside right of the legend and flip its rotation so it reads in the requested direction.
- Render the Pareto Explorer `3D Projections` color-scale metric label vertically beside the legend, matching the chart-axis style so long metric names stay readable without clipping.
- Shift the Pareto Explorer `3D Projections` color scale and its top-right metric label left into the reserved right margin so neither the legend title nor the tick labels get clipped at the card edge.
- Move the Pareto Explorer `3D Projections` color-scale metric label out of the narrow Plotly colorbar title and render it as a separate top-right annotation so the legend text and tick values stay readable.
- Move the Pareto Explorer `3D Projections` color-scale title above the legend so long metric names no longer sit awkwardly beside the vertical bar.
- Restore the shared selected-config detail section below both Pareto Explorer `Overview` and `Explorer`; it had accidentally ended up inside the Explorer-only stage markup.
- Return the Pareto Explorer detail expanders to their normal collapsed-by-default behavior after the temporary debugging change.
- Render Pareto Explorer `3D Projections` color scales via Plotly `layout.coloraxis` so the selected `Color by` legend is shown reliably in the small linked projection panels.
- Fix the Pareto Explorer `Full Configuration` text color by overriding the page-level `pre` style so the shared JSON panel keeps the same green code text as Backtest.
- Move Explorer colorbars inward and reserve extra right margin so `3D Projections` and `3D Scatter` do not clip the selected `Color by` scale at the card edge.
- Fix Pareto Explorer `3D Projections` to use the selected `Color by` metric for the linked XY/XZ/YZ charts too, so the Plotly color scale now appears there as expected.
- Align the Pareto Explorer shared `Full Configuration` panel spacing with Backtest so the shared green read-only JSON viewer renders the same way in both places.
- Remove the Pareto Explorer `Bot Parameters` section and switch `Full Configuration` to the same shared read-only JSON panel used by Backtest, with shared copy/expand/font-size controls.
- Show a Plotly color scale on all Pareto Explorer charts whenever `Color by` is set, including 2D scatter, 3D scatter, and the linked 3D projection charts, with a shared per-chart value range.
- Fix Pareto Explorer 3D/WebGL charts after fullscreen exit by restoring both the remembered Plotly height and the chart wrapper/DOM element height, preventing the scene from overflowing over the text below.
- Fix Pareto Explorer 3D projection charts so each linked 2D projection renders inside a fixed chart wrapper and resizes against that wrapper instead of overflowing out of the Explorer card.
- Remove the unused top placeholder area in Pareto Explorer `3D Projections`; the main Explorer chart wrapper is now hidden entirely while the three linked 2D projection charts are shown below.
- Add a Pareto Explorer `3D Projections` toggle so the linked XY/XZ/YZ charts can be shown either stacked or side by side, with an immediate Plotly resize after switching.
- Move the Pareto Explorer `3D Projections` side-by-side control into the Explorer title row and render it as a real switch instead of a plain checkbox.
- Restore the Pareto Explorer chart height after leaving fullscreen by remembering each chart's original Plotly layout height and reapplying that height on fullscreen exit.
- Restore the Pareto Explorer chart fullscreen button by wrapping the main Explorer/projection charts in `.chart-wrap` containers again and relayouting Plotly on fullscreen changes.
- Preserve the currently visible Explorer axes every time a quick view switches to `Custom...`, even when those current metrics are outside the filtered custom-options list.
- Move the Pareto Explorer detail-panel `Explorer Score` into the `Trading Style` section so only true robustness data remains in the `Robustness` block.
- Clarify Pareto Explorer scoring by renaming the detail-panel `Overall Score` to `Robustness Score` and adding the live slider-weighted `Explorer Score` beside it.
- Fix Pareto Explorer 3D axis and hover labels to use the same human-readable metric names as the 2D Explorer charts.
- Show the selected config detail panel below the Pareto Explorer charts too, so Explorer keeps the same config inspection area as Overview.
- Preserve the Pareto Explorer stage and Deep Intelligence tab from the URL on browser refresh so reloading on Explorer no longer jumps back to Overview.
- Fix Pareto Explorer Explorer slider races so live preference updates ignore stale Playground/detail responses and keep the selected config synced to the current Best Match.
- Fix dashboard position `Next DCA` and `Next TP` values for short positions by classifying open buy/sell orders with the correct side-aware logic and nearest-price selection in both snapshot and live API paths.
- Fix the dashboard orders chart entry-line profit/loss color for short positions so it no longer uses long-only price-vs-entry comparisons.
- Add regression tests covering long/short dashboard order classification so nearest DCA/TP price selection stays correct for both snapshot and live API helpers.
- Make the dashboard Orders widget hedge-aware without a DB migration by sending the selected position side through `/ws/candles` and `/dashboard/orders_data`, showing `Orders: unknown` for hedged DB snapshots, and replacing that placeholder once live exchange order metadata can identify the correct leg.
- Start the standalone FastAPI Pareto Explorer migration with a new `/api/pareto-explorer/main_page` route, 3-column HTML shell, guide integration, and bootstrap session endpoint for side-by-side validation against the legacy Streamlit explorer.
- Add new FastAPI Pareto Explorer entry points from Optimize Results: a sidebar button for single selected results, a sidebar button in the pareto list view, and double-click on a result row's pareto-count cell.
- Extend the new FastAPI Pareto Explorer shell with real session/load data: ini-backed defaults, fast/full load controls, live summary metrics, and first-class result metadata from `ParetoDataLoader`.
- Add first migrated Command Center data to the FastAPI Pareto Explorer with champion ranking, migration-safe insights, and config detail endpoints backed by `ParetoDataLoader`.
- Extend the new FastAPI Pareto Explorer right detail panel with live selected-config metrics, risk profile, and full config JSON rendered from the new FastAPI endpoints.
- Extend the FastAPI Pareto Explorer detail panel with live bot-parameter and scenario-metric sections for the selected config.
- Fix the FastAPI Pareto Explorer bootstrap script so version and serial placeholders are injected as valid JavaScript strings and the page can actually initialize/load data.
- Fix the FastAPI Pareto Explorer page-template placeholders for token and API/bootstrap values so the served JavaScript no longer ships raw `%%...%%` tokens and the sidebar/actions can initialize.
- Reshape the FastAPI Pareto Explorer layout to match the original Streamlit flow more closely by removing the extra right-side detail sidebar and moving selected-config details into the main Command Center content area.
- Bring the FastAPI Pareto Explorer selected-config section closer to the Streamlit original with a config selector, four-part overview, inline expanders for full config and metrics, and API-backed config-option data.
- Wire the FastAPI Pareto Explorer "Run Backtest with this Config" action to the existing FastAPI backtest draft flow so selected configs open directly in the Backtest editor.
- Wire the FastAPI Pareto Explorer sidebar "Run Backtest" button to the same selected-config backtest draft flow instead of leaving it on the migration placeholder handler.
- Wire the FastAPI Pareto Explorer sidebar "Seed Selected" button to open Optimize V7 with the selected config as a draft-based starting seed.
- Wire the FastAPI Pareto Explorer sidebar "Seed Whole Result" button to open Optimize V7 with the result's full `pareto` directory as the starting seed path.
- Wire the FastAPI Pareto Explorer sidebar actions for CSV export, original PB7 Pareto Dash, and PB7 3D Plot using the existing FastAPI/Optimize launch flows and a local overlay viewer.
- Start the FastAPI Pareto Playground migration with a real preference-weighted best-match flow and a live 2D Plotly scatter fed by a new `/playground` API endpoint.
- Start the FastAPI Pareto Explorer Deep Intelligence migration with a live `Parameters Near Bounds` view backed by a new `/deep-intelligence/parameters` API endpoint and wired into the normal page reload/load flow.
- Extend the FastAPI Pareto Explorer `parameters` Deep Intelligence tab with a live parameter-influence heatmap, `Top N Parameters` control, and Plotly near-bounds chart rendered from FastAPI JSON payloads instead of Streamlit widgets.
- Extend the FastAPI Pareto Explorer `scenarios` Deep Intelligence tab with a real scenario-metric selector, scenario comparison boxplots, and per-scenario mean/std/min/max statistics from a new `/deep-intelligence/scenarios` API endpoint.
- Extend the FastAPI Pareto Explorer `evolution` Deep Intelligence tab with weighted/BTC metric filtering, smoothing-window controls, and a clickable optimization timeline from a new `/deep-intelligence/evolution` API endpoint.
- Extend the FastAPI Pareto Explorer `correlations` Deep Intelligence tab with migrated selection strategies (`Top Performers`, `Diverse Styles`, `Risk Spectrum`) and a clickable risk-profile radar from a new `/deep-intelligence/correlations` API endpoint.
- Rework the FastAPI Pareto Explorer layout to be denser and closer to Streamlit by restoring command-center preview charts, shrinking champion cards, and collapsing bot-parameter sprawl into a compact grid expander.
- Extend the FastAPI Pareto Playground toward Streamlit parity with visualization-mode controls, quick views, color selection, restored command-center preview payloads, and live 2D/3D/3D-projection/radar Plotly outputs from the `/playground` API.
- Fix the FastAPI Pareto Playground chart settings so visualization, quick-view, color, and toggle changes immediately reload the chart, and keep `Color by: None` from being silently replaced by a fallback metric.
- Clean up the FastAPI Pareto Explorer sidebar by removing the extra draft/plot actions that do not belong there and replacing them with a dedicated `Load all_results` action that toggles full-load mode through the existing loader flow.
- Tighten the FastAPI Pareto Explorer sidebar again by removing `Reload` and `Export DataFrame`, while restoring `Run Backtest` and keeping `Load all_results` as the only additional sidebar action.
- Make the FastAPI Pareto Explorer `Back to Optimize` action return directly to the Optimize `results` panel instead of the generic optimize landing state.
- Fix the FastAPI Pareto Explorer preview metric mapping to mirror the currently visible Streamlit behavior for real runs, so the preview charts use the same `adg_usd` / `omega_ratio_usd`-style axes instead of switching to weighted variants and showing different points.
- Bring the FastAPI Pareto Explorer preview closer to the Streamlit presentation by restoring a dedicated preview weighted toggle, forcing dark Plotly backgrounds instead of white panels, and clearing the leftover placeholder-style preview text when charts render.
- Make the FastAPI Pareto Explorer preview keep the currently selected config visibly highlighted across preview/playground refreshes so selected Pareto stars do not disappear behind the generic point cloud.
- Fix the FastAPI Pareto Explorer `Back to Optimize` button to return to the actual Optimize `results` panel by restoring the `#results` hash that the Optimize page uses during bootstrap.
- Fix a FastAPI Pareto Explorer playground regression where preview/playground selection highlighting broke 3D projection rendering and surfaced as a misleading bootstrap failure after the first `/playground` call returned HTTP 500.
- Make the FastAPI Pareto Explorer `Load all_results` sidebar action visibly show that a full load is in progress and report the actual loaded total/result window so full-load mode no longer looks like a no-op.
- Add a dedicated full-load status card to the FastAPI Pareto Explorer so `Load all_results` shows explicit idle/loading/loaded/error phases instead of relying only on button text and summary chips.
- Tighten the FastAPI Pareto Explorer summary-card typography so long result names fit better, and move `Load Control` into a dedicated sidebar-accessible `Settings` section to keep the Command Center focused on analysis.
- Rebalance the FastAPI Pareto Explorer summary cards to a `2:1:1` layout and remove the redundant `Stage` card so the `Result` card gets enough width for long result names.
- Fix the FastAPI Pareto Explorer `Load all_results` guard so changing `Max Configs` or load strategy allows a fresh full-load instead of incorrectly claiming the full result is already loaded.
- Fix Optimize V7 result-list `Modified` timestamps to use stable result artifacts (`all_results.bin` or pareto JSONs) instead of the result-directory mtime, so Pareto Explorer cache writes no longer make results look newly modified.
- Fix the FastAPI Pareto Explorer full-load progress card so it leaves `Loading` after the load request completes instead of staying stuck until a later unrelated render pass.
- Re-enable the FastAPI Pareto Explorer `Load all_results` sidebar button immediately after the core full-load request completes, instead of keeping it disabled while follow-up analysis refreshes continue in the background.
- Refine the FastAPI Pareto Explorer full-load progress card into real phases (`load`, `command center`, `playground`, `deep intelligence`) so `Loaded` and button re-enable happen only after all dependent refreshes finish.
- Smooth the FastAPI Pareto Explorer full-load progress bar with a small trickle animation between phase updates so the bar moves less abruptly while still only completing when the real refresh pipeline is done.
- Replace the FastAPI Pareto Explorer fake/trickle-only `all_results` progress with a real backend load job that reports `ParetoDataLoader.load(progress_callback=...)` status through a polling API, and mirror that live status in both Settings and the main Command Center view.
- Harden the FastAPI Pareto Explorer full-load polling contract so the UI keeps polling until the finished backend job actually exposes its payload, instead of surfacing a false `Full load completed without payload` error.
- Fix the FastAPI Pareto Explorer full-load polling loop so the initial `Load all_results` request truly waits for the backend job to finish, instead of resolving on the first intermediate poll and falsely reporting a missing payload.
- Refine the FastAPI Pareto Explorer post-load refresh pipeline with visible substeps for selected-config and Deep Intelligence refreshes, and skip the redundant intermediate Playground reload that previously made the Command Center phase look stalled.
- Move the FastAPI Pareto Explorer full-load refresh pipeline fully server-side so Command Center, selected config detail, Playground, and the active Deep Intelligence tab are all computed inside the background load job with real backend step updates instead of follow-up browser fetches.
- Remove the extra FastAPI Pareto Explorer `Result Snapshot` block and show the same live full-load progress bar directly inside the `All Results` summary card while loading.
- Collapse the FastAPI Pareto Explorer `All Results` status card after loading so the large progress block only appears during load/error states, while the compact `Loaded` chip stays inline next to the result count.
- Remove the redundant auto-generated Pareto Explorer info banner after load and relabel the summary metric from `Pareto Files` to the actually relevant `Pareto Front` count.
- Fix the FastAPI Pareto Explorer `Pareto Front` summary metric to match Streamlit by showing the Pareto count of the currently visible view slice after full load, instead of the global persisted Pareto-front count from `load_stats`.
- Fix the FastAPI Pareto Explorer preview charts to match Streamlit by plotting the visible full-load slice and only marking that slice's locally visible pareto-optimal configs as stars in the preview, instead of using the full loaded set's global Pareto markers.
- Add a main-content `Display Range` control to the FastAPI Pareto Explorer and wire it into the existing server refresh path so visible counts, `Pareto Front`, config selector/champions, and preview/playground charts all follow the current full-load slice without re-running a background full load.
- Make the FastAPI Pareto Explorer `Display Range` control auto-render on slider release and numeric-field changes, removing the extra `Apply` step while still avoiding duplicate full-mode refresh requests.
- Fix the FastAPI Pareto Explorer full-load refresh bundle so browser-like range updates no longer corrupt visible-slice `is_pareto` flags during selected-config/detail/deep-intelligence refreshes, which previously made the UI report too few Pareto configs for the same visible range.
- Fix `ParetoDataLoader.ensure_details()` to preserve already-known Pareto membership from the initial load, so lazy detail loading in both Streamlit and FastAPI no longer drops visible-slice Pareto counts after champion/detail/insight rendering.
- Reorder the FastAPI Pareto Explorer Command Center so `Top Champions` and `Insights` appear above the Plotly preview charts.
- Tighten the FastAPI Pareto Explorer `Top Champions` cards so the config number stays readable and each champion fits into a compact two-line layout.
- Simplify the FastAPI Pareto Explorer selected-config detail view by removing the debug/bootstrap and duplicate metric sections, surfacing `gain` and `max_drawdown` in the main metrics block, and hiding Scenario Metrics unless real scenario data exists.
- Remove the FastAPI Pareto Explorer `Select Config` debug dropdown from the selected-config panel so detail changes now happen only from the Top Champions list and chart selections, matching the intended user flow.
- Remove the redundant selected-config subtitle line under the FastAPI Pareto Explorer config number, since trading style and robustness are already shown in the detail cards below.
- Simplify the FastAPI Pareto Explorer selected-config header further by removing the `Selected Config Details` label and right-side `Refresh` button, leaving only the config number as the header.
- Shorten the FastAPI Pareto Explorer selected-config title from `Config #...` to just `#...`.
- Remove the duplicate selected-config backtest button from the FastAPI Pareto Explorer detail area, keeping the sidebar action as the single backtest entry point.
- Improve the FastAPI Pareto Explorer preview charts by using readable axis labels and explicit axis-title spacing so the left/bottom plot scales stay visibly labeled in the dark Plotly preview.
- Remove a redundant FastAPI Pareto Explorer bootstrap-time Playground reload that briefly re-rendered the preview charts with a second payload and made the text above the Plotly previews flicker.
- Remove redundant internal Plotly titles from the FastAPI Pareto Explorer preview charts so the external summary line above each chart is the only preview heading shown.
- Stop the generic FastAPI Pareto Explorer load-summary renderer from writing loader/debug text into the left preview summary line, which briefly overwrote the intended Plotly preview heading during page refreshes.
- Rename the FastAPI Pareto Explorer sidebar `Command Center` stage to `Overview` and move the preview color scale/legend apart so the left Plotly preview no longer overlays legend text with the color bar.
- Expand the Pareto Explorer help topics in English and German with a dedicated Overview-page guide that explains how to interpret Top Champions, preview charts, robustness/performance tradeoffs, and the Explorer score chips.
- Fix the left Pareto preview so only one color scale is rendered at a time, preventing the color bar from overlapping legend text after `Load all_results`, and add fullscreen buttons for both Overview preview charts.
- Fix a Pareto Explorer frontend bootstrap regression where the new preview fullscreen helper embedded raw `</script>` tags inside the page's inline script block, causing the browser to stop parsing the rest of the page script.
- Drop the custom Pareto preview fullscreen overlay/buttons again and switch the FastAPI charts back to Plotly's built-in modebar controls, matching the existing Streamlit/PB7 interaction model more closely.
- Separate the left preview color scale and legend more aggressively for full-load views, and add a direct Plotly overlay expansion path on preview-chart double-click so large-chart inspection works even when the local Plotly bundle does not expose a visible fullscreen modebar button.
- Replace the Pareto preview double-click expansion again with the same browser-fullscreen style used elsewhere for charts, and pull the left preview color scale back closer to its original position while only nudging the legend slightly to avoid overlap.
- Match the Pareto preview fullscreen affordance to the Backtest results charts by using the same custom Plotly modebar fullscreen button, remove the extra inline expand icon, and keep the left preview color scale visible while only shifting the legend slightly upward to avoid overlap.
- Make the Overview preview charts taller and move their legends above the plotting area so the charts can use the full width without overlaying legend content inside the graph itself.
- Force the Pareto Explorer Overview previews to reapply their legend-above-chart layout in the frontend render path too, so stale cached payload/layout merges cannot leave the legend pinned beside the graph.
- Add a Pareto Explorer sidebar action to leave `all_results` mode again and switch back to the original Passivbot pareto-only config set without manually opening Settings.
- Reuse the cached full `all_results` loader when switching back from Passivbot pareto-only mode, so returning to full mode does not trigger a second expensive reload while the cache is still warm.
- Add an Overview preview checkbox that defaults to Pareto-front-only points and can be toggled to show all visible configs in the two preview charts without affecting the main playground mode.
- Apply the same dark Plotly layout wrapper to the Pareto Playground charts and 3D projection subcharts so they no longer fall back to Plotly's white default theme.
- Preserve `_w` in visible metric labels so the Pareto Playground and preview axes correctly show weighted metric names when weighted mode is enabled.
- Resize the Pareto Playground Plotly charts after the tab becomes visible so the first switch into the Playground uses the full available width instead of keeping a hidden-container width.
- Align the Pareto Explorer sidebar container and resize-handle initialization with the standard Optimize page pattern so the sidebar can be widened like the other FastAPI pages.
- Restore `#main-content` flex growth after the sidebar layout alignment so the Pareto Explorer uses the full remaining window width beside the resizable sidebar.
- Rename the former `Pareto Playground` tab and primary UI labels to the simpler `Explorer` wording while keeping the existing internal stage keys unchanged.
- Make the FastAPI `Efficiency` and `Trading Efficiency` quick views use the same weighted/currency-aware exposure metric resolution as the legacy Streamlit explorer, restoring parity for those presets.
- Add the missing `Custom...` Explorer quick-view workflow for 2D Scatter, 3D Scatter, and 3D Projections, including explicit axis selectors and mixed weighted/currency filtering like the legacy Streamlit implementation.
- Stop the Explorer preference sliders from snapping back while dragging by updating only their live value labels on `input` instead of re-syncing the whole form each tick.
- Fix the Explorer preference sliders so valid `0` values are preserved instead of being coerced back to their old defaults by `||` fallback logic.
- Add explicit shared range-input track/thumb styling in the Pareto Explorer so slider endpoints render cleanly and visually align with true `0` and `100` positions.
- Restore the blue filled portion on the Explorer sliders by driving the range track fill from live slider percentage updates instead of relying on browser-specific accent rendering.
- Fix Explorer weight parsing on the FastAPI backend so valid `0` slider values reach the Best Match scoring logic instead of being coerced back to the old `80/60/70` defaults.
- Restore Streamlit-style Explorer preference behavior by removing the manual refresh button, refreshing the Best Match live while dragging the sliders, and auto-syncing the selected config to the current Best Match.
- Rename the FastAPI Pareto Explorer summary-card count label from `Pareto Optimal` to `Pareto Front`, because the card shows the size of the non-dominated set rather than the property of a single config.
- Remove the redundant `Open Guide` button from the FastAPI Pareto Explorer Settings load-control section, leaving guide access to the shared global help entry points.
