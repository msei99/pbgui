# v1.95.9

## Fixed

- Detect active PBCluster, PBRun, PBData, PBCoinData, and PBMonitorAgent processes running an outdated code serial and restart only those managed daemons before restarting the API server from the shared navigation control.
- Respect explicit master-to-master sync peer allowlists while allowing masters to synchronize directly with every active VPS, so intentionally unreachable coordinators are skipped instead of reported as errors.
