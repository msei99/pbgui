# v1.84

- Dashboard Positions: add a `Manage` workflow for reduce-only market closes plus PB7 forced-mode sync actions (`Panic`, `Graceful Stop`, `Take Profit Only`) per symbol or for all positions of a user.
- Dashboard Positions: harden direct market-close execution across Bybit hedge mode, Binance hedge/one-way mode, Hyperliquid vault/min-value handling, and Bitget/GateIO/OKX reduce-only params.
- Dashboard Positions: fetch live positions, balances, close prices and open orders directly from exchanges with database fallback snapshots.
- Dashboard Positions: improve the Manage UI with live in-place row refreshes, stable drag position, better sizing, preview windows, duplicate-action blocking and input-focus safety.
- Guide: document Dashboard Manage and PBv7 Run forced-mode actions for Panic, Graceful Stop and Take Profit Only in EN and DE.
- V7 Run: load instance backups into the V7 editor as drafts, including backup coin override files, instead of restoring and syncing them directly.
- V7 Run: avoid losing approved coins when a backup draft is saved before the coin/tag selector finishes initializing, and refresh the coin-overrides editor asset used by the run editor.
- V7 Run: add per-instance `P`, `G` and `T` safety-confirmed actions to sync Panic, Graceful Stop or Take Profit Only for all positions.
- Dashboard Balance and Positions: use Hyperliquid account state for live vault/user values instead of stale database snapshots or the wrong wallet view.
- Dashboard editor: refresh inline Balance and Positions widgets from live REST endpoints instead of the older live stream payloads.
- Dashboard editor: reload stale in-memory dashboard renderer versions instead of reusing an old `DashRender` object in already-open tabs.
- Dashboard Orders: fetch live open orders for Positions DCA/TP values and initial Orders chart lines, with database fallback.
- Dashboard live widgets: show relative freshness (`now`, `Ns ago`) instead of ambiguous clock timestamps.
- Optimize V7: keep Save & Queue with a changed name as a new config and queue item instead of renaming existing queued configs.
- Strategy Explorer: load the PB7 Rust extension from the configured PB7 virtualenv path so clean PBGui environments do not need Passivbot Rust installed.
- Log Viewer: resolve remote PB7 history entries from `pb7/logs/...` through the configured PB7 log directory so older bot logs open again.
- VPS Manager: prevent duplicate same-host deploys from corrupting Ansible runner artifacts by blocking overlapping playbooks per host and cleaning only each run's private data directory.
- Backtest V7: adjust the local `sl_bt` config to use roughly double practical wallet exposure through larger initial entry sizing.
- Backtest V7: add local `sl_bt_twe_mid` as a moderate wallet-exposure variant based on the original `sl_bt` result.
- Optimize V7: add local `sl_bt_twe_target` config with bounded wallet-exposure targets, tighter grid spacing bounds, and drawdown/loss-ratio limits.
