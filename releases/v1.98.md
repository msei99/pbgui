# v1.98

## Added

- Added PB8.1's official collision-aware market identifiers, strategy- and HSL-aware typed coin overrides, and definitive staged-bundle validation through the installed PB8 runtime.
- Added PB8.1 Run controls and v8.0-to-v8.1 migration UX for WebSocket forager candles, unavailable-symbol cooldown, order-replacement churn gates, and diagnostic startup budgets.
- Added Bitunix and WEEX PB8 credentials, runtime-derived exchange selectors, and read-only Dashboard balance, position, order, OHLCV, fill, and PnL integration. Direct close actions remain fail-closed until exchange-specific live tests are complete.

## Fixed

- Fixed the Dashboard Positions widget alternating between DB fallback and live exchange values during refreshes.
- Fixed PB8.1 Optimize objective, scoring, and limit scenario bases being dropped by the visual editor, preserved unknown fields, and included effective suite bases in checkpoint-resume compatibility checks.
- Preserved sparse PB8 overrides through Pareto, archive, queue, and backtest handoffs; made Optimize archive and queue repairs recoverable; returned official migration reports; and isolated PB8 backtests from API service restarts.
- Hardened exchange credentials so browser authentication uses the HttpOnly cookie, credential responses remain masked, explicit reveal uses a no-store POST, backup diffs are redacted, and secrets remain replacement-only.
- Removed browser-readable Dashboard session tokens and preserved exact PB8 market identities in Dashboard forced-mode actions.
- Reduced V7 Cluster config activation latency with bounded targeted materialization and immediate catch-up for changes arriving during an active PBCluster pass.
