# v1.92.26

- Make cluster retention a continuous enabled/disabled policy that automatically retains the configured history window and removes unreachable blobs.
- Remove the 24-hour activation and blob-stability waits while preserving replica, checkpoint, migration, replay, and crash-recovery safety gates.
- Show the automatic retention lifecycle, checkpoint acknowledgements, blockers, retained/deleted counts, and last evaluation without manual reports.
- Clarify optional node diagnostics with deterministic required-blob-set digests and explicitly local garbage counts.
