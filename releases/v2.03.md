# v2.03

## Fixed

- Rebuild and verify the PB8 Rust extension during PB8 updates instead of trusting a source stamp that could mask an outdated binary.
- Show an actionable PB8 runtime rebuild message when optimizer metadata detects mismatched Python and Rust schemas.
- Include the large Backtest, Optimize, and VPS Manager HTML pages in GitNexus analysis by raising the project file-size limit to 1 MB.

## Changed

- Support PB8's separate per-individual and per-variable mutation controls on CPU and GPU, preserving automatic values and legacy compatibility; document schema 8.4 EMA paths, coupled unstuck search, and bounded risk-input recovery.
- Bump the API restart serial for the completed PB8 compatibility update so running APIs expose the restart-required action.
- Use original, locally installed PB8 documentation for parameter tooltips across Run, Backtest, and Optimize, with canonical path matching, source attribution, scrollable long explanations, and no generic runtime placeholders.

## Validation

- Complete offline test suite: 8,137 passed, 48 skipped.
- API restart serial: 2610.
