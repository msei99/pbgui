# v1.98.18

## Fixed

- Fixed PB8 Optimize returning a raw HTTP 500 when the PB8 runtime is unavailable after an incomplete update. The page now renders and leaves the existing metadata endpoint to report the actionable runtime error.
