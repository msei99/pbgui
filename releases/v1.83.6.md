# v1.83.6

- Agent instructions: require explicit confirmation before any command that modifies a bot/VPS host, including pulls, deploys, stashes, service restarts, or file changes.
- VPS Manager: keep local masters online and suppress rclone/PBRemote warnings when PBRemote is intentionally unconfigured.
- Optimize Editor: keep Limits numeric fields editable without resetting partial input to `0` while typing.
- Optimize Editor: remove the temporary `loss_profit_ratio_w` scoring blocker again so PBGui no longer rejects that metric.
- Optimize Queue: add mini actions in the Pareto Front log card to jump to matching Paretos or Pareto Explorer.
