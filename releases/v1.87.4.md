# v1.87.4

- Fixed Optimize page Configs and Queue tables so title/name headers can sort rows.
- Added a Run Editor sidebar copy action to save the current PBv7 config disabled for another user, including referenced per-coin override files and target-based version increments when overwriting an existing target config.
