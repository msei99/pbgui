# v2.04.3

## PB8 Scenario Editor

- Configure Training and Holdout windows directly over a local OHLCV price chart. Move, resize and draw windows; drag between lanes to change their role.
- Automatically exclude Holdout periods from Training, including splitting affected windows. Preserve distributed Holdouts with local and Vast results for later validation.
- Add snapping, shared-boundary resizing, independent edge handles, undo/redo and drag-to-trash. Date and duration labels remain readable with generous mouse targets.
- Use mouse-wheel zoom, Shift-drag pan and automatic candle detail when zoomed in. Reference sources are limited to configured coins; daily aggregation is cached and vectorized.
- Apply validated windows directly to the Suite while preserving scoring, limits and aggregation. Remove redundant preview tables and editing controls; keep instructions in the guides.
- Restore the saved Optimize editor directly after browser refresh without flashing the config list.

## Vast.ai uploads

- Use deterministic compressed archives and resumable 2 MiB blocks to reduce retransmission on unreliable connections.
- Replace the ten-minute total transfer cap with receiver-progress monitoring and a 120-second idle timeout. The rental deadline and completion reserve still bound uploads.
- Show checksum-verified progress, current-block receiver acknowledgements and transfer speed separately. Report sanitized SSH failure categories without exposing raw diagnostics.

## Profit Sweep

- Allow cancellation of owned, unsubmitted test transfers. Automatically retire expired, unsubmitted Vault signature requests so they do not indefinitely block Live evaluation.

## Documentation

- Update English and German guides for graphical scenarios, distributed Holdouts, upload progress and prepared test-transfer recovery.
