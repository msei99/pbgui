# v1.92.13

- Keep Cluster Sync idle by skipping managed credential scans during periodic maintenance and preventing transient compatible process heartbeat changes from republishing migration ACKs.
- Treat mailbox messages which expire between index and fetch as a benign synchronization race instead of failing the entire peer pass.
