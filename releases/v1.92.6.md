# v1.92.6

- Fixed credential migration freeze-generation churn by electing exactly one deterministic active master as coordinator while all other masters remain participants.
- Fixed batch updates and branch switches on remote masters so their API unit is synchronized and restarted safely after code changes, preventing stale PBApiServer processes from blocking migration acknowledgements.
