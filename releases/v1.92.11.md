# v1.92.11

- Reduced Cluster Sync CPU load by sharing one validated oplog snapshot across bounded peer workers, coalescing self-generated sync triggers, and running the managed credential scan only once per cycle.
- Delegated VPS fanout to the deterministic coordinator so secondary masters synchronize only through that coordinator instead of duplicating every remote transfer.
- Reconciled Python requirements before allowing regular PBGui update restarts and applied the AsyncSSH compatibility constraint to VPS environments, allowing normal updates to recover affected remote masters.
