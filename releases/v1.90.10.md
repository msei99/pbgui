# v1.90.10

- Prevented remote command injection through VPS log line counts and bot instance names.
- Prevented remote log path traversal and VPS inventory writes or deletion outside the hosts directory.
- Enforced SSH host-key verification and exact fingerprint confirmation while preserving trusted hostname aliases for IP-based VPS connections.
- Added VPS Overview key-status indicators aligned with the preferred confirmed key type and a responsive sidebar dialog to review, trust, or explicitly replace SSH host keys and reconnect monitoring without leaving the GUI.
