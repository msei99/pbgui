# v2.01.3

## Fixed

- Persist compact PB7 and PB8 Backtest result summaries in a reconstructable owner-only SQLite index, rebuilding only new or changed results and falling back to direct file reads if the cache is unavailable.
- Keep Backtest result lists on compact JSON summaries only: never stream large balance/equity CSV histories during list loading, mark gain-derived Final Balance values with `~`, and leave unavailable Final Equity as `—` until lazy detail loading.
- Load all compact Backtest result summaries in one request per version and remove the duplicate final table render, while keeping charts, fills, analysis, and configs lazy.
- Preserve Backtest Results row selections by stable result path across slow progressive page loads and table re-renders instead of storing selection only in transient DOM classes.
