# v1.86.12

- Added a Cluster Sync nodes table Login Key status and renamed the per-host action to Install Key so missing dedicated Cluster SSH keys are visible and repairable per host, including temporary password retry and immediate PBCluster retry after key repair.
- Preserved the selected Cluster Sync sidebar section across browser refreshes for all Cluster menus.
- Automatically runs the remote probe, refreshes the Cluster nodes table, and verifies Login Key status after per-host Install Key or Repair All SSH completes.
- Added a real Repair All SSH progress job with a pollable progress bar and persistent result details.
- Fixed the Repair All SSH progress bar layout so it stays full-width and compact.
