# v2.01.13

## Fixed

- Run approved PBGui AI optimizer-dataset Python with capability-free Landlock/seccomp isolation, explicit compact multi-scenario matrices, and restart-safe bounded proposal persistence so hosts that deny Bubblewrap UID mappings remain supported.
- Raise the still-bounded Python analysis budget to 30 CPU seconds and 45 wall seconds so complete multi-period Pareto matrices finish on slower masters.
- Retry one transient timeout for the optional post-approval AI summary while preserving the already persisted action result and never rerunning the approved action.
- Allow up to 1000 Pareto candidates and 1000 reviewed backtest jobs in one proposal, including configured timeframe jobs plus Holdout and Full validation together, and automatically open their Results Compare after the exact queued jobs finish.
