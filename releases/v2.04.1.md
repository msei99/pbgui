# v2.04.1

## Changed

- Default untouched new PB8 configurations to 20,000 exact evaluations on their first switch to Vast. Preserve saved and explicitly edited iteration values; document that stagnation can stop earlier but is not guaranteed.

- Remove the obsolete Vast job dropdown, raw status blocks and secondary log viewer with their event handlers/CSS. Keep job selection in JavaScript state and feed worker activity directly into the shared Optimize dashboard.

- Open the contextual Optimizer Settings guide from Settings, including Vast account setup for PB8. Add English/German API-key creation steps, required permissions, billing-read guidance and connection troubleshooting.

- Document a read-only retrospective Vast convergence analysis, including checkpoint/final-front verification, later improvements and the difference between per-result and snapshot-based stopping.

## Fixed

- Assess convergence again from the verified final Pareto snapshot, including final evaluation counts and result-import retries. Preserve the actual stop reason and show completed or unavailable final checks instead of indefinitely pending evaluations.

- Review Vast UI issues #326–340: prevent duplicate worker/requeue/delete actions across refreshes, add deletion confirmation and validation retry, normalize empty GPU filters, refresh incompatible offers and show selection/supervision feedback. Distinguish provider credential failures from PBGui session expiry, report non-JSON HTTP errors clearly, and preserve/display billing refresh failures. Regression-test the unified queue/log workflow instead of exposing obsolete standalone controls.
