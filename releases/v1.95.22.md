# v1.95.22

## Added

- Added combinable `TradFi only` and `No downloaded history` filters to the Hyperliquid Build Best 1m coin picker; current-day Hyperliquid files no longer hide TradFi symbols that still need a Tiingo backfill.

## Fixed

- Restored the focused Hyperliquid Build Best 1m and Download l2Books panels by sending their section as a valid iframe query parameter instead of requesting a nonexistent JSON route.
- Limited Hyperliquid TradFi build choices to canonical mappings that the Tiingo downloader can actually fetch.
