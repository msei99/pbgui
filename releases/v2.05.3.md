# v2.05.3

- Updated new Vast.ai rentals to the temporary PB8 HSL+heartbeat worker image. Long CPU exact
  populations and starting-config evaluations now emit bounded progress after each five-minute
  quiet interval, while existing rentals remain pinned to their original recoverable images.

- Fixed PB8 Vast.ai optimizer queue deletion so removing jobs permanently removes their complete
  retry lineages, retained performance history, stored job artifacts, and local logs instead of
  revealing older retry attempts; bulk selections are now deleted in one efficient batch, and
  concurrent polling no longer leaves a stale job-read error visible.
