# v1.88

- Investigated OKX OHLCV REST and archive data coverage for BTC/ETH market-data downloads.
- Added a temporary OKX BTC-USDT-SWAP 1m NPZ download speed-test script under /tmp/opencode.
- Rechecked and filled missing OKX BTC-USDT-SWAP archive minutes from REST in the temporary NPZ output.
- Benchmarked OKX archive and REST parallelism for the planned market-data downloader.
- Documented the final OKX market-data integration plan under docs/plans/okx-market-data.md.
- Verified OKX SWAP volume fields and fixed the planned bv mapping to use volCcy/base volume.
- Added OKX USDT-SWAP 1m market-data downloads with archive/REST repair, latest refresh, queue/status APIs, frontend selection, inventory/source-index integration, and focused tests.
- Changed manual Best 1m history builds to use available exchange coins instead of requiring coins to be enabled for automatic latest refresh.
- Fixed OKX 1m source-index display so official OKX archive data is shown as API data instead of other_exchange.
- Optimized OKX archive writes by aggregating UTC archive buckets and batching archive volume enrichment before writing daily NPZ/source-index files.
- Optimized OKX archive volume enrichment by deriving missing base volume from OKX contract metadata before falling back to REST.
- Optimized OKX history builds further by using monthly archive ZIPs with daily tail files while keeping the stable REST limiter.
- Added an OKX Best 1m multi-coin pipeline so the next coin can start REST work while the current coin is processing archives, sharing one OKX REST limiter across the job.
- Fixed the OKX 1m heatmap year overview so it reads the OKX source index instead of showing No data.
- Fixed Market Data Queue worker restart to wait for the old worker process to exit before starting a replacement.
- Validated local OKX BTC-USDT-SWAP 1m candles against Binance USDT perpetual data and OKX REST spot checks to confirm UTC day bucketing and base-volume mapping.
- Compared local BTC 1m candles across Binance, Bybit, OKX, and Hyperliquid to verify cross-exchange UTC alignment and identify isolated Hyperliquid data-quality outliers.
- Improved local Services controls by using real systemd restart actions, disabling duplicate clicks while actions run, and reporting final service state when systemd actions fail or time out.
