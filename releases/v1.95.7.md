# v1.95.7

## Fixed

- Reload PBCluster automatically when a deployed runtime serial changes so an already current local checkout cannot keep running stale Cluster Sync worker code.
- Repair remote checkpoint-tail sequence gaps before handshake and allow normal Cluster bundles enough time to finish during automatic backlog convergence.
