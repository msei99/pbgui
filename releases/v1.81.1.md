# v1.81.1

- Change: Keep PBData market-data refresh status in the API server memory cache instead of rewriting `data/logs/market_data_status.json`.
- Change: Keep PBData fetch summaries in the API server memory cache instead of rewriting `data/logs/fetch_summary.json`.
- Change: Keep PBData poller metrics in the API server memory cache instead of rewriting `data/logs/poller_metrics.json` every metrics cycle.
- Fix: Keep Optimize queue warnings in `OptimizeQueueAPI.log` instead of grouping them into `PBGui.log`.
- Change: Suppress PB7 config-load INFO noise in the API server, route uncategorized Python warnings to `PBApiServer.log`, and cache Optimize queue seed metadata between refreshes.
- Change: Route API-server SSH logging to `data/logs/SSH.log`, suppress AsyncSSH routine INFO noise by default, and stop inheriting console stdout/stderr.
- Fix the Streamlit master cleanup helper so running it with `sudo` cleans the invoking user's crontab via `SUDO_USER` instead of checking root's empty crontab.
