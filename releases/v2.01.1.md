# v2.01.1

## Fixed

- Bound PB8 optimizer `all_results.bin` cold scans by bytes and wall-clock time per status request, and expose incremental history-scan progress so long-running jobs no longer freeze the dashboard for minutes after an API restart.
- Fixed fresh PB8 live activation sending only the instance config to its assigned VPS; the fast Cluster bundle now includes and projects the current exchange API keys first, preserving Hyperliquid fields such as `is_vault`, so PBRun does not remain blocked by a missing PB8 `api-keys.json`.
