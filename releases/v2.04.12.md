# v2.04.12

- Establish a mandatory PBGui-wide rule in AGENTS.md for all existing and future interfaces: automatic data updates, no in-app manual refresh controls, and preservation of the complete navigation context on browser refresh and automatic updates.

- Detect Vast.ai instances deleted outside PBGui during active rentals using two fresh successful absence checks at least ten seconds apart. Provider errors reset confirmation; stopped instances and changed ownership labels cannot falsely clear a rental lock. Preserve the creation visibility grace period and never rent a replacement automatically.
- Finalize unfinished jobs on a verified closed shared rental, clear its active-job pointer and pause the affected queue after unexpected instance loss while preserving queued jobs and local result snapshots.

- Detect Hyperliquid Unified/Portfolio Margin in Transfers, API Keys and Profit Sweep; explain the Standard/Manual requirement, display shared USDC instead of misleading Futures zero, and keep unsupported transfers blocked. Vault routes remain independently validated.
- Add Max to Transfers to fill the selected route’s available amount while retaining explicit review and confirmation.

- API Keys: direct users to Hyperliquid Account Type → Manual (Standard) with an Open Hyperliquid link. Remove the experimental account-mode mutation endpoints and browser-wallet signing integration; retain mode detection, shared-balance display and Profit Sweep guidance. Keep long account-mode notices readable within the page width.

- Link the API Keys account-mode notice directly to Hyperliquid Portfolio, where Account Type is available.

- Show Hyperliquid Standard/Manual Spot USDC balances in API Keys and add inline Spot/Perps transfers with Max, fresh availability checks, explicit confirmation and retained operation IDs for uncertain results. Reuse the existing Transfers backend and refresh balances after confirmation.

- Automatically refresh API Keys account usage on browser focus so removed Run instances no longer leave a stale In Use badge. Ignore outdated list responses; retain backend deletion safeguards.

- Automatically reconcile delayed manual-transfer confirmations throughout API operation, rather than only at startup. Refresh inline Spot/Perps balances while confirmation is pending and clear resolved operation locks automatically. Keep Transfers balances/history current without a Refresh button; never resubmit an existing transfer.

- Replace the generic PB8 rollout/update error with cause-specific messages for failed sync, stale status, missing handshake, and explicitly unsupported PB8 replication. Preserve the HTTP 409 publication safeguard; malformed sync timestamps now produce a useful blocked status.

- Remove the unintended mandatory VPS password from the pre-apply swap probe. Use SSH keys when no session password is available and clear any temporary password after the read; preserve authoritative swap checks and password-free PBGui/PB8 update paths.

- Explain recognized ChatGPT turn failures with actionable, secret-safe categories instead of a generic response-failed message. Retain turn-scoped error notifications when the completion omits details; never infer a Free-plan ban from an unknown failure.

- Support multiple owner-isolated ChatGPT subscription profiles with independent server-side logins, profile names, account/plan details and available usage limits. Preserve the legacy login as Default; persist each conversation's profile with no automatic quota failover. Add profile creation, rename, disconnect and removal while retaining history. Restore profile/chat navigation on browser reload and automatically update status without a manual refresh control.

- AI Chat: select individual ChatGPT profiles directly in the Provider menu, synchronized with the sidebar, models and conversation profile binding.

- Fixed the compact AI drawer to list ChatGPT profiles separately, fetch profile-specific models and create chats with the selected profile; existing chats retain their profile.

- Fixed oversized page-context checkbox in the AI drawer by isolating its size and label layout from host-page form styles.

- ChatGPT usage now shows remaining percentage and a progress bar with monthly, weekly, daily or 5-hour limit labels instead of misleading minute counts.

- Fixed stuck ChatGPT login attempts: completion clears pending state, and explicitly starting login replaces the current user’s old attempt so a fresh login link can be displayed. Other users’ logins remain protected.

- Added automatically updating provider usage to the AI drawer and OpenCode Go usage to the full chat page. Go shows the provider-reported 5-hour, weekly and monthly limits; ChatGPT usage is requested for the selected profile. Zen explicitly reports that balance must be viewed in its console.

- Compacted AI drawer usage: account on its own line, limit and remaining percentage on one row, and each usage bar spanning the full available width.

### Prioritized open-issue fixes

- #357: Lock Inventory delete-selected and clear-dataset actions through confirmation and completion; retain the reviewed exchange and dataset if navigation changes.
- #324: Deduplicate Job Monitor delete/retry/requeue actions and preserve HTTP status for non-JSON error responses.
- #319: Lock Service settings saves and show readable FastAPI field-validation errors.
- #318: Deduplicate Hyperliquid cancel/retry/requeue actions and reload running jobs automatically after cancellation.
- #317: Validate heatmap download queue HTTP responses and job IDs; display the actual backend error.
- #310: Lock DB sync-job deletion and retain the reviewed job ID while a confirmation is open.
- #306: Stop AI polling, invalidate stale responses and clear busy/pending state after conversation deletion.
- #304: Lock root-login submissions to prevent accidental parallel failed attempts; restore entry after failure.
- #302: Keep other AI proposals visible while reviewing one; restore the selected card after cancelling approval.
- #301: Lock Welcome setup saves until the request completes, including recovery after errors.

- Refresh release regression checks for the shared AI usage asset, safe settings messages and existing logging ownership; distinguish read-only ZIP members from append-mode file writes in the audit.
