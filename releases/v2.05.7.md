# v2.05.7

- Add **Information → Hyperliquid Limits** with a cross-VPS view of running PB7/PB8 bots, their wallets, account request counters, and 24-hour history. The VPS running a bot now samples each local Hyperliquid wallet once every five minutes; Masters consume agent snapshots instead of independently querying the exchange.
- Show a one-time current account request limit when opening a saved Hyperliquid API key. Accounts without running bots receive no periodic polling or history.
- Let VPS Monitor buy an exact number of Hyperliquid request credits (1–100,000) from a saved main account, with the USDC cost and explicit confirmation. PBGui verifies the limit after a confirmed purchase and blocks API restart while the action is in progress.
