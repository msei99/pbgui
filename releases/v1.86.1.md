# v1.86.1

## Fixes

- PBGui update and branch-switch playbooks now repair missing PBCluster systemd units even when the git checkout is already current.
- Systemd unit sync failures during PBGui updates now fail visibly instead of being masked, so missing `pbgui-pbcluster.service` cannot leave Cluster Sync silently inactive.
