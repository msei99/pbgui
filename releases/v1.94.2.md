# v1.94.2

- Fixed master updates for legacy installations by restoring a missing `role = master` setting without overwriting explicit roles, so the PB8 install action becomes available after updating again.
