# v1.92.15

- Reduced idle Cluster credential previews to snapshot and catalog checks for already materialized or non-recipient secrets, while retaining full envelope validation for new generations and historical signing keys.
- Fixed secondary-master migration deduplication to prefer an already published CMC credential and retire matching legacy-shadow records left pending by earlier migration attempts.
