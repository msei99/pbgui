# v1.92.14

- Fixed secondary-master credential migration so legacy CMC sources that already match a fully published Cluster credential are accepted and safely cleaned without republishing the credential through a conflicting pending lifecycle.
- Reduced idle Cluster CPU usage by reusing current atomically persisted state snapshots, replaying and validating the full signed oplog only after a new operation, and loading local operations only when a peer is actually behind.
