# v1.99.6

## Changed

- Renamed VPS Manager bulk runtime actions to explicitly identify Passivbot and PB7/PB8.

## Fixed

- Prevented the global AI drawer from rescanning large log-viewer DOM trees every 500 ms; live context chips now use a lightweight page summary while full controls are collected only for an actual AI request.
- Allowed the registered Welcome page key in validated AI page catalogs, preventing `Invalid page context` from blocking Kimi K3 and other models before a turn starts.
- Fixed bulk Passivbot updates using stale persisted runtime profiles, skipping a PB8 installation confirmed by current host telemetry, and hiding the per-host PB7/PB8 command behind a generic progress label.
