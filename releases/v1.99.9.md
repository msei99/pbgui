# v1.99.9

## Fixed

- Added adaptive ChatGPT Codex capability control: semantically identical calls reuse cached results, unique information-gathering calls remain available beyond a 32-call soft budget, four repeated arguments or unchanged result digests trigger a final answer, 64 calls remain the hard loop guard, and high-effort turns receive a five-minute deadline.
- Hid AI proposal cards immediately after a confirmed approval or rejection while showing the server-side action status, restoring the card only when the decision request fails.
- Added an exact AI Backtest Results Compare action that validates managed result resources, selects the corresponding PB7/PB8 result rows, and opens the existing Compare chart instead of relying on unreliable generic control clicks.
