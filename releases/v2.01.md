# v2.01

## Added

- Add a Pareto-sidebar Holdout action that reconstructs immutable Sweep holdout dates and prepares standalone PB8 Backtest queue drafts for all selected candidates.
- Evaluate Sweep Cycles as real sequential Pareto cash flows with carry, sweep-reset, refill-reset, cooldown gaps, immutable result sidecars, and selectable synthetic metrics.
- Stream real Exchange/Coin-pair progress for PB8 OHLCV start-date lookups and provide an isolated Stop action with deterministic API-shutdown cleanup.
- Add a PB8 Optimize start-date lookup for explicit approved coins, with earliest-selected-market and common-all-markets OHLCV coverage modes plus strategy warmup.
- Add deterministic PB8 Rolling Windows, Walk-Forward, and Sweep Cycles scenario previews to Suite Mode, with training-only apply, separate holdout provenance, bounded per-exchange expansion, and a matching read-only PBGui AI capability.

## Fixed

- Restructure the bilingual Scenario Generator guide around action effects, setting meanings, the complete Sweep workflow, result/holdout handling, and explicit behavioral boundaries.
- Interpret PB8 `gain_strategy_eq` correctly as the terminal `end/start` multiplier in Sweep cash-flow calculations instead of adding one a second time.
- Mirror Sweep Long approved coins to the disabled Short side for PB8 Suite compatibility, remove mirrored Short ignores, validate symmetry before Save/Queue, and write fixed selectors with real `long.*` bound keys.
- Normalize all Long Sweep bounds by effect on Apply: keep meaningful non-zero strategy/filter/risk/unstuck ranges active, fix zero-width and disabled-HSL ranges, and fix one-coin Forager ranking weights without touching Short.
- Size Sweep Long `n_positions` from the explicit approved-coin count and set the high-risk Long TWE search range to `6..10` on Apply, without changing Short or unrelated bounds.
- Reduce Sweep `backtest_completion_ratio` with `min` instead of `mean`, ensuring any incomplete training scenario triggers the 99% completion limit.
- Render PB8 Suite `median` and `std` reducer options explicitly so Sweep Cycles no longer displays its saved median reducer as mean; keep PB7 methods unchanged.
- Apply a focused high-risk Sweep scoring preset with Gain/Sortino/Drawdown objectives, an 80% Drawdown cap, 99% completion floor, and Suite Aggregate basis.
- Synchronize PB8 `backtest.starting_balance` from the Sweep Cycles policy on Apply and reject Save/Queue when the simulation and cash-flow balances diverge.
- Increase Scenario Generator preview-table and orange warning text from extra-small to the standard small text size for readability.
- Preserve the Suite Mode expander's open state across Scenario Generator Preview, Recalculate, and other internal re-renders even before Suite Mode is enabled.
- Add a Scenario Generator Recalculate action beside Guide so OHLCV-driven date or exchange changes immediately refresh automatic Sweep counts and invalidate stale previews.
- Make Sweep Cycles derive Stride and the maximum complete Training-window count automatically from date coverage, cooldown, and reserved Holdouts instead of requiring manual arithmetic.
- Preserve Approved Coins, Ignored Coins, and Tags edits made while the initial Optimize market resolver is still loading, so the first clear action is not undone by a late response.
- Normalize PB8 OHLCV inception recommendations to date-only values and replace the oversized start-date selector with compact `1st` and `All` buttons beside `start_date`.
- Add a concrete bilingual Sweep Cycles workflow example with policy interpretation and explicit simulator boundaries.
- Open the version-specific PB7/PB8 Optimize guide instead of the first generic Optimize match, and link the PB8 Scenario Generator directly to its bilingual guide section.
- Keep running PB8 CPU/Pymoo progress current during long duplicate-candidate stretches by preferring the verified optimizer's durable `all_results.bin` evaluation count over stale Pareto-update log counters.
- Document the complete PB8 Scenario Generator workflow, template choices, Preview/Apply behavior, holdout handling, and example window plans in both guides.
- Add the standard underlined hover help to every PB8 Scenario Generator setting.
- Make the VPS Manager modal header X use an explicit click target isolated from the draggable header, matching the working footer Close button.
- Keep the local VPS Manager Master row on the PBGui version loaded by the running API instead of allowing a stale monitor-agent cache to display an obsolete version while update colors use current commit data.
