# v2.0.4

## Fixed

- Fixed PBGui/API restarts leaving a legacy persistent VPSMonitor process active after monitor code updates; the shared restart prompt now detects the missing release-cache capability and reloads VPSMonitor before the API.
- Made multi-result **Add to Archive** copy in one transaction and update only the affected manifest entries instead of rescanning and rehashing the complete archive; compact progress text no longer overflows the Backtest sidebar, and pending own-archive changes expose **Git Push** directly in Results.
- Telegram release notifications now honor temporary rate-limit responses and can replay a specific version manually instead of dropping release notes after posting the commit notification.
