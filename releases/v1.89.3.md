# v1.89.3

- Hardened FastAPI CORS defaults, moved shared Help overlay REST auth to Bearer headers, and removed leftover HLDA debug artifacts.
- Replaced frontend CDN dependencies with local static assets for offline-safe page loading.
- Documented the no-external-frontend-dependencies rule for future UI work.
- Moved remaining REST fetch token auth to Bearer headers, logged previously silent VPS monitor exceptions, and moved the Bybit data probe into scripts.
- Fixed the VPS instance collector logging path so the PBv7 Run page receives valid monitor JSON instead of staying on collecting status.
- Fixed Cluster Sync V7 materialization so local run_v7 directories covered by tombstones are backed up and removed instead of lingering in PBv7 Run.
