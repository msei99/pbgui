# v1.92.30

- Collect cluster-wide retention health automatically during normal PBCluster peer sync instead of requiring a manual browser verification scan.
- Make operation-history expiry deterministic across replicas by using signed operation age plus the committed safe baseline rather than local receipt time.
- Report actual retained oplog counts and prevent local cleanup health from appearing as a healthy cluster-wide result.
