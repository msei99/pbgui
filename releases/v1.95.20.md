# v1.95.20

## Added

- Added an exchange/coin-selectable PBGui MarketData close-price overlay to PB7 and PB8 backtest balance/equity charts.

## Fixed

- Resolved normalized backtest coin names to their canonical PBGui OHLCV directories, exposed configured exchanges for PB8 combined and suite results, measured price coverage against the visible chart range, and made the price trace visually distinct.
- Kept the combined PBGui/runtime update action green unless both components have updates, leaving single-component warnings on their individual sidebar buttons.
