# v1.86.4

## Fixes

- Hardened PBGui update and branch-switch playbooks so notified systemd setup/restart handlers still run when a later task fails after updating the checkout.

## Docs

- Clarified Cluster Sync migration docs: VPS nodes must be set to Reachable via SSH, repaired with Repair SSH, and probed before Join appears.
