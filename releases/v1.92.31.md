# v1.92.31

- Show remaining local garbage after completed blob cleanup instead of presenting already deleted candidates as still present.
- Keep duplicate or stale materialized CMC credentials out of the active pool when they are absent from Cluster desired entries.
- Reduce idle PBCluster CPU load on passive VPS replicas and treat mailbox expiry races as completed delivery windows instead of peer failures.
- Redirect application logging during Pytest runs into per-test temporary directories instead of production service logs.
- Render VPS Manager host snapshots and local metric history without waiting for deep probes, reject stale detail responses, and attach action logs to their exact run transcript.
