# v1.98.10

## Fixed

- Fixed PB8 `PNL Tdy` counting historical fill batches as today's result after bot startup; PBGui now attributes individual fills by their embedded exchange timestamp and invalidates the old PB8 daily-PnL cache.
