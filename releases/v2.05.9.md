# v2.05.9

- Fixed Hyperliquid limit sampling on PB8-only VPS hosts. When a running bot account is absent from the PB7 API-key file, the monitor agent resolves it from the existing local PB8 API-key projection. This lets the VPS sample those wallets and send their counters to the central limits overview.
