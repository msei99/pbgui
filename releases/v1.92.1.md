# v1.92.1

- Fixed VPS Manager task logs staying blank when an update command for a remote master is remapped to the master playbook.
- Fixed completed VPS deploy runs remaining at `starting` because progress parsing still searched the legacy task-log directory.
- Decoupled the credential protocol barrier from unrelated API serial bumps; fresh processes now qualify by protocol and capability generation while retaining their serial for diagnostics.
