# v1.98.9

## Fixed

- Fixed stale VPS Manager `Reboot` badges after successful Linux-update reboots by refreshing package status after the optional reboot and rejecting reboot requirements older than the current host boot.
