# v2.05.6

- Cache the legacy PBGui cron migration check in the VPS monitor agent until the cron spool changes or one hour passes, avoiding a `crontab -l` call every ten seconds.
- Reuse authenticated SSH transports for repeated Cluster Sync peer commands with bounded OpenSSH multiplexing.
- Make the daily VPS cleanup read the legacy bot status before removing its directory, and remove timestamped PB7 run logs 30 days after their last write even when that status is unavailable.
