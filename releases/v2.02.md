# v2.02

## Fixed

- Allow PBGui AI to patch existing PB8 Optimize configs while preserving their authoritative PBGui runtime provenance, without permitting model-authored metadata, secrets, or host paths.
- Add a persistent pin control to switch the global PBGui AI drawer between overlay and side-by-side layouts without hiding the active page.
- Reserve dynamic space above large Backtest Compare plots so wrapped legend entries no longer cover the result curves.
- Let PBGui AI add one or more exact managed Backtest results, including a finished Holdout, to the current Results Compare selection instead of requiring the model to reconstruct the complete selection.
- Refresh the Profit Sweep account sidebar immediately after Live activation and show a dedicated loading state instead of briefly rendering schema defaults while switching accounts.
- Align new Profit Sweep policy defaults with the reviewed UI values for trigger, sweep share, transfer minimum and rounding, Live baseline, and Hyperliquid Vault minimums while preserving disabled startup and exchange-specific assets.
- Prevent a newly created From Enable policy using Include Dry Period from treating all historical account PnL as immediately sweepable when no Dry evaluation initialized its baseline.
- Add a universal fixed-route Transfers page for idempotent internal movements across Hyperliquid Vault/Main/Spot, Hyperliquid Perps/Spot, Bybit Unified/Funding, Binance USD-M/Funding, and Bitget Futures/UTA/Spot, with Direction beside Amount, route-specific balances, explicit Main Unified guidance, isolated history, reconciliation, and a preselected Profit Sweep funding handoff.
- Distinguish total Vault account value from the user's actual Hyperliquid max-withdrawable amount and show sanitized open-position details so collateral and potential Passivbot sizing impact are visible before review.
- Poll delayed exchange history after accepted Profit Sweep Live submissions so normal ledger propagation does not immediately pause a policy as Unknown, without ever repeating the transfer request.
- Make the Dry Decision Journal collapsible and default it closed for Live or Paused Unknown policies while preserving manual disclosure state during same-account refreshes.
- Replace Profit Sweep's Evaluate now, global account-refresh button, and duplicate Account Preview panel with selected-account background reads that keep existing values visible, expose compact freshness/error state, clarify pending Sweep Due and HWM recovery, serialize with real-funds actions, and retry only while visible.
- Show Profit Sweep's authoritative next scheduled check as a live countdown with local time and list sanitized open positions from the automatically refreshed account snapshot with explicit Passivbot collateral-sizing guidance.
- Block scheduled Live sweeps while any manual transfer for the same account remains unresolved, preventing new real-funds work until exchange outcome is known.
- Bound the complete PBGui AI-patched PB8 config and prevent a delayed preference response from reverting a newer AI drawer pin action.
- Keep the Tiingo monthly-cache regression test deterministic at month boundaries and make long-running transfer tests refresh their synthetic snapshot timestamps.
