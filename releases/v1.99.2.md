# v1.99.2

## Fixed

- Opened a newly named PB8 Optimize config automatically after a successful PBGui AI save instead of leaving the source config in the editor.
- Kept the refresh generation-safe so a delayed AI completion cannot overwrite a newer manual editor navigation.
- Added one generic, page-advertised AI action bridge that can navigate between registered PBGui pages, wait for destination data, and reuse the existing Optimize, Backtest, and bot log viewers for exact selected or active entities instead of adding feature-specific model tools or proposing Python analysis.
