# v1.97.1

## Fixed

- Fixed existing systemd installations without the new VPS Monitor unit to request and perform a one-time safe daemon handoff after the migration code is loaded, instead of continuing with API-owned SSH sessions that disconnect on every restart.
