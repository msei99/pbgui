# v1.95.14

## Fixed

- PB8 live runners now reject unstamped or stale Rust extensions before launch, validate the selected extension through PB8's own fingerprint logic, and report an actionable `blocked` reason instead of crash-looping behind a generic `sync needed` status. Rerunning **Update PB8** remains the safe repair because it rebuilds, stamps, and validates the extension.
