# v1.98.30

## Improved

- PB8 Optimize Results now avoid cold-decoding every `all_results.bin`, scan each Pareto directory once, and use bounded first-record metadata so result sets remain immediately visible after API restarts.
- Resume and Continue retain strict full-stream validation while the visual Results list uses only bounded metadata.
