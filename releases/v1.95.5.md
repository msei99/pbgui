# v1.95.5

## Fixed

- Split large remote Cluster operation pushes into bounded progress batches, report actionable timeout errors, and rebuild remote materialized state when a retry finds all operations already present.
- Prevented VPS metadata refreshes from demoting joined replicas or replacing their authenticated signing key, restore replica state only after an identical remote Cluster identity is verified, and keep crypto-registered nodes labeled as resumable.
- Verified rewrapped credential envelopes against the winning recipient-update actor rather than the original provider publisher.
