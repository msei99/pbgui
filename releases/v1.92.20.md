# v1.92.20

- Load CMC pool and lease status from the validated materialized cluster snapshot instead of repeatedly replaying the full oplog.
- Avoid the redundant CMC usage request on the Services pool tab because the pool response already contains the same usage summary.
- Preview converged V7 materialization before applying it so periodic PBCluster turns no longer rebuild the full oplog without changes.
- Skip the expensive credential migration security rescan during normal turns after the local migration state is durably complete.
- Promote accepted legacy-shadow credentials to cluster ownership once so TradFi projection fingerprints remain stable instead of reapplying every turn.
