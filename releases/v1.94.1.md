# v1.94.1

- Added full PBv8 Optimize parity through the shared PBv7 editor, including every installed PB8 strategy, runtime-derived optimizer controls, official V7 migration, and fresh, Pareto-seeded, or exact-checkpoint launches.
- Added the PBv8 Optimize backend and detached runtime with runtime-derived metadata, secure config bundles and immutable queue snapshots, distinct fresh/Pareto/checkpoint starts, PB8 results and Pareto APIs, and global PB7/PB8 automatic-start arbitration.
- Shared the existing PB7 Optimize frontend with PB8 through a version adapter covering nested config paths, runtime metadata and controls, version-owned logs/results/paretos, and exact checkpoint resume routing.
- Made the shared Pareto Explorer PB7/PB8-aware with managed result ownership, compressed PB8 result decoding, nested preset bounds, version-routed Backtest/Optimize handoffs, and cookie-only browser authentication.
- Added version-safe PB8 Backtest and Optimize archives under each config version, including mixed-version browsing, import/export, rebacktest, and immutable Retest & Replace scheduling.
- Prevented automatic PB7/PB8 optimizers from overlapping each other or an already running manual optimizer while continuing to allow explicit parallel manual starts.
- Fixed PB8 Optimize editor initialization so scoring, limits, runtime overrides, and every strategy-specific bound slider render, and made strategy changes load and preserve the matching runtime-provided bot defaults and bounds.
- Fixed Backtest CPU settings so the dialog loads the authoritative host CPU limit before allowing slot changes.
- Added PB8 version, branch, and upstream status to VPS Manager Overview and host detail headers.
- Unified PB7 and PB8 Optimize queue settings in one persisted configuration and applied shared CPU and PBGui market-data overrides safely to launch copies.
- Unified PB7 and PB8 Backtest queue settings and automatic CPU slots across both versions, and made the shared Settings dialog open immediately while authoritative values refresh in the background.
- Made PB8 materialized-cache cleanup honor PB8 operation and per-run locks, conservatively preserving active, foreign, or malformed lock owners.
- Labelled PB8's verified detached upstream checkout as `master` instead of `unknown` in VPS Manager.
- Fixed PB8 Pareto Dash layout loading behind the authenticated proxy, retained its original native presentation inside movable and resizable window chrome, secured its credentials and response headers, and bounded API-owned sessions with idle reaping, durable verified orphan recovery, and shutdown-safe process-group cleanup.
- Hardened PB8 Optimize queue recovery with actionable launch failures, stale-PID safety, crash-consistent requeue/reorder repair, startup artifact reconciliation, and Services Monitor controls.
- Fixed running PB8 optimizers being terminated with `pbgui-api.service` by launching them in independent transient user-systemd units with runner-published durable ownership.
- Fixed Pareto Explorer stopping before session bootstrap with an undefined version helper, which left PB7 and PB8 result pages empty.
- Fixed PB8 Backtest and Optimize OHLCV checks and preloads to use PB8 paths, planner modules, virtualenv, and native download command, with update locking and runtime-aware config cache invalidation.
- Completed PB8 Optimize result parity with suite-aware Pareto projections and 3D plots, strict transactional resume readiness, active-result deletion guards, incremental progress caching, full queue performance status, and accurate config/result metadata.
- Completed PB8 shared Optimize editor parity for authoritative universes, lossless multi-strategy and future override editing, fractional polish and native pymoo defaults, artifact-gated result actions, transactional checkpoint resume, median suite views, and stale-response protection.
- Fixed PB8 Optimize bound sliders so runtime default search ranges no longer overwrite the wider parameter ranges, allowing values such as `n_positions = 1` and native minima for nested strategy, risk, HSL, forager, and unstuck bounds.
- Fixed PB8 Optimize CPU controls staying at `max 1` when an early queue WebSocket update arrived before the slower runtime-backed settings response; host CPU metadata now merges without overwriting newer queue settings.
- Kept the shared Optimize autostart CPU value editable while **Override config CPU** is disabled, so the next override value can be prepared without activating it.
- Made **Save** refresh the immutable snapshot of the queue row from which an Optimize config was explicitly opened, so edits such as `optimize.n_cpus` persist when that row is reopened or started.
- Returned queue-opened Optimize editors to the Queue panel on **Home** or **Save** instead of dropping users into Configs.
- Fixed PB8 Optimize result deletion to block only the exact optimizer-owned result while preserving queue and Pareto Dash conflicts, and made atomic batch deletion retain actionable errors through filesystem races.
- Made PB8 Pareto lists expose canonical gain and requested objective statistics through a bounded compact cache, isolate active file churn, bound suite payloads, and render large sortable fronts with one browser DOM commit.
- Opened complete PB8 Optimize result drafts directly in the shared Backtest editor without re-preparing and rejecting the exact Pareto configuration during handoff.
- Rejected PB8 Optimize search spaces that can activate Forager volume or volatility scoring while allowing the required EMA spans to reach zero, while retaining zero for safely disabled signals.
- Kept the required Long/Short HSL enable switches visible when PB8 runtime metadata contributes additional Optimize runtime overrides.
- Rendered PB8 Optimize runtime overrides in separate Long/Short groups with canonical HSL paths, friendly labels, and a constrained `always`/`threshold`/`never` restart-policy selector instead of free text.
- Fixed PB8 Optimize HSL enable and no-restart threshold controls to persist in their schema-defined bot-side HSL sections, migrated previously submitted misplaced overrides during save, and removed a redundant second PB8 prepare when refreshing an opened queue snapshot.
- Made Dashboard Panic, Graceful Stop, and Take Profit Only push and materialize their V7 config immediately on the assigned Cluster bot host, and report success only after the remote host accepts the new version instead of waiting for periodic reconciliation.
- Kept the PB8 Optimize Results panel available while the running optimizer atomically replaces Pareto files, instead of returning HTTP 500 when a scanned candidate disappears before its modification time is read.
- Kept PB8 Forager volume and volatility EMA span sliders at `1` or above, using **Fixed** rather than zero-valued bounds to exclude those parameters from optimization.
- Removed GUI stalls caused by DB Sync repeatedly decoding the full completed-task archive, PB8 Optimize list views cold-decoding complete result streams, and V7 Edit and Run rebuilding optional Cluster/CMC host diagnostics; simplified **Enabled On** to host names, kept raw fetch results out of PBData monitoring payloads, moved periodic queue/status work off the API event loop, coalesced market-status updates, cleaned up shared navigation watchers on page changes, and paused PB8 log-status polling in hidden tabs while preventing overlapping requests.
- Serialized DB Sync scheduler and API state changes, made PB8 cross-page drafts and archive imports race-safe, and retained durable active-optimizer result ownership before allowing deletion.
- Kept PB8 OHLCV preloads alive across API restarts in independent transient user-systemd units and preserved disabled PB8 sides unchanged when generating Pareto presets.
