# v1.94.6

- Added persistent interval schedules for OHLCV Copy Data jobs so multiple optimizer systems can be refreshed automatically, run on demand, and managed from the Market Data page.
- Fixed PB7 Rust rebuilds in combined master updates, VPS updates, and fresh VPS setup by passing maturin an explicit virtual environment without shell activation; combined master updates now defer their detached API restart until all PB7 handlers finish.
