# v2.02.9

## Fixed

- Prevented the Hyperliquid OHLCV build coin picker from remaining permanently empty when it is opened during market-data discovery or XYZ mapping refreshes.
- Retried explicitly transient empty discovery responses while preventing stale configured coins from reaching OHLCV jobs.
- Distinguished pending discovery, missing enabled coins, pending XYZ mappings, and permanent Tiingo mapping exclusions instead of displaying one misleading error.
- Made authentication errors, malformed responses, detached pages, and superseded requests terminate without stale UI updates or unnecessary retries.

## Validation And Limitations

- Offline Market Data, picker UI, authentication-boundary, configuration-sync, guide-coverage, and release suite: 464 passed, 27 skipped.
- Automatic discovery retries are bounded to twelve attempts over approximately one minute; after exhaustion, a reload starts a new check.
- No bot/VPS host was deployed, updated, or restarted during development or release preparation.
