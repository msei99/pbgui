# v1.94.3

- Fixed standalone masters without pre-existing Cluster state becoming restart-blocked during credential migration; they now initialize private local protocol state without joining a remote Cluster.
