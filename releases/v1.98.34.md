# v1.98.34

## Fixed

- The shared PB7/PB8 Optimize progress dashboard no longer crashes when an iteration target exists before the first evaluation is reported.
- PB8 OHLCV readiness now accepts standard virtualenv Python symlinks while still requiring the native `passivbot` CLI to resolve inside that virtualenv.
