# v2.01.5

## Fixed

- Allow Hyperliquid Vault sweeps within the conservative withdrawable cap when `alwaysCloseOnWithdraw` is enabled, make Margin Buffered usable with active positions, and let Flat Only ignore resting orders while still requiring zero position size.
- Keep PB8 Optimize result responses when users open another panel during a slow metadata request, show explicit loading/refresh feedback, and retain already loaded result rows while refreshing.
