# v1.92.7

- Fixed a host-metadata probe overload loop on low-resource Cluster nodes by reading persisted Cluster snapshots instead of replaying the complete oplog for telemetry.
- Deduplicated concurrent host-metadata collection per master and added a remote collector timeout so failed SSH probes cannot leave accumulating Python processes behind.
