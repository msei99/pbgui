# v1.95.12

## Fixed

- VPS PB8 updates now stamp and verify the freshly built Rust extension against the checked-out source before declaring the live runtime ready, and PBRun preserves early PB8 startup output in the instance error log.
- PB8 instance logs now wait for the saved `enabled_on` assignment and render the requested remote host immediately instead of briefly or persistently falling back to an unrelated local bot log.
