# v1.98.33

## Fixed

- PB8 Backtest-to-Run drafts now detect an existing user config and require a versioned Replace-and-Save confirmation, preserving backups and the normal config-version increment instead of silently remaining a rejected new draft.
