# v2.01.10

## Fixed

- Add an exchange-neutral Profit Sweep transfer-rounding step that always rounds down after caps and retains the fractional remainder as due.
- Reconcile successful Hyperliquid Vault withdrawals when its ledger floors `requestedUsd` exactly one micro-USDC below the signed amount, without broadening matching beyond that provider quantum.
- Preserve each PB8 Pareto validation draft's configured exchange group so Combined optimizer scenarios are not split into artificial single-exchange jobs with no valid coins.
- Open PB8 Pareto validation confirmations over the Queue panel instead of misleadingly landing on Configs.
