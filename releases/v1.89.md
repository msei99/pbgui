# v1.89

## Fixed

- Added visible per-result progress to PBv7 Results `Add to Archive`, changed
  the archive copy loop to process selected results sequentially, and moved the
  backend archive copy work off the API event loop so the UI remains responsive.
