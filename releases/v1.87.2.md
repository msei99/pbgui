# v1.87.2

- Fixed Config Archive pulls when a clone is behind the remote but has local generated metadata changes plus preserved archive content under `pbgui/configs/`.
- Added Config Archive coin metadata, a coin filter for archived backtest results, and an own-archive config rename action for versioned backtest groups.
- Added coin display and filtering to the generated interactive Config Archive score table, and removed the low-signal confidence column from generated score tables.
