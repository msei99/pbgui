# v1.98.20

## Fixed

- Fixed the persistent PB8 update warning not appearing because the earlier settings request failed before metadata loading. Expected PB8 runtime 503 responses now activate the banner and suppress the transient toast.
