# v2.03.3

## Fixes

- Allow retrying a saved VPS swap target after a failed apply: prompt for a password, compare against active remote swap, and protect settings reads from concurrent inventory refresh. Report swap read failures instead of treating them as zero swap.

- Keep saved VPS swap settings visible after browser reload by waiting for full configuration before rendering editable fields.

- Protect VPS swap/firewall apply starts from inventory refresh overwriting the selected command, and open the exact run log including its initial output.

- Clear the active dashboard iframe and its load callback after deletion (#264).
- Preserve account-specific transfer routes when refreshing previews (#261).
- Confirm discarding unsaved Profit Sweep policy changes before switching accounts (#260).
- Show failed Hyperliquid history Delete, Retry and Requeue requests (#259).
- Confirm Balance Calculator config replacement and preserve edits on cancelled, failed or stale loads (#258).
- Handle Coin Data load errors without unhandled promise rejections (#257).
- Verified that the v2.03.2 Market Data request guards prevent stale exchange settings from overwriting the current selection (#256).
- Protect unsaved DB Sync Job drafts on close, new and row changes; avoid background editor replacement (#254).
- Report rotation save failures and retain Logging Monitor settings across view changes (#253, #252).
- Replace the failed PBData settings spinner with a visible error and retry action (#251).
- Preserve backup-retention edits against delayed load and save responses (#246).
- Preserve Strategy Explorer simulation plots across stage navigation and serialize Simulation/Compare execution; tolerate absent movie export controls (#244).
- Scope Run action completions to their initiating modal, block repeated conversions and sort version/TWE values consistently (#240).
- Release DB Tools action buttons on every outcome and resume interrupted status polling without duplicating an operation (#237).
- Preserve custom Pareto Z metrics across 2D/3D switches (#232).
- Refresh Dynamic Ignore previews from the actual tag multiselect change callback (#225).
