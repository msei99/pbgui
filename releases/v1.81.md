# v1.81

- Complete the FastAPI Strategy Explorer migration with the Analysis, Simulation, Compare, Movie Builder, Raw Config, export, progress, and local guide workflows now served by PBGui's API/web stack.
- Restore direct PBv7 menu navigation to the FastAPI Pareto Explorer page.
- Refresh remaining Pareto Explorer preview wording now that the FastAPI page is the active implementation.
- Align Strategy Explorer simulation and compare behavior with PB7 for current HSL, Forager, market-order, warmup, result-handoff, fills.csv, and candle-window cases while keeping remaining edge-parity work documented for concrete mismatches.
- Remove the legacy Streamlit Strategy Explorer and Live vs Backtest pages, their navigation/dashboard entry points, and the obsolete Streamlit custom components.
- Remove the remaining Streamlit application entrypoints, startup wiring, dependency pins, `Config.py`, and obsolete Streamlit-era config tests now that PBGui runs through FastAPI.
- Remove legacy `st_base` and port-8501 compatibility from FastAPI routes, frontend navigation, setup firewall rules, and active help/code wording.
- Move PBGui auth secrets to `data/auth/secrets.toml` with one-time fallback import from the old `.streamlit/secrets.toml` path so existing installations keep their password during upgrade.
- Add a master cleanup helper for upgraded systems to stop stale Streamlit processes, remove old autostart entries, close UFW port 8501, uninstall direct Streamlit packages from detected PBGui venvs, and remove obsolete `.streamlit/config.toml`.
- Clean up tracked Streamlit config and local cache artifacts, tighten Python/Pytest cache ignores, and refresh setup, README update guidance, release, roadmap, and English/German help documentation for the FastAPI-only UI.
- Keep PB7 branch/version metadata, Exchange credential validation, Hyperliquid stock-perp best-1m behavior, and market-data tests aligned with the current FastAPI/PB7 integration.
