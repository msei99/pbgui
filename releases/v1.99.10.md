# v1.99.10

## Added

- Added a read-only PB8 Legacy Results panel for valid artifacts below `<pb8dir>/backtests/*` outside `pbgui`, including `combined` output, with symlink-safe bounded discovery plus existing details, charts, files, and Compare while keeping Rebacktest, Add to Run, and Delete disabled.
