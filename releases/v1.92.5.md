# v1.92.5

- Fixed credential migration deadlock by restarting an already active PBCoinData service during rolling updates even while credential capability is still unknown; inactive collectors remain untouched.
