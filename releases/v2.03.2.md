# v2.03.2

## Fixes

- Batch legacy income imports in one transaction and maintenance lease (#290).
- Fail unsuccessful DB Sync recovery attempts explicitly and recover stranded pending tasks (#288).
- Drain push WebSocket receivers, senders and authentication watchdogs on disconnect, logout and cancellation (#282).
- Ignore stale Help topic, language-index and search responses (#266).
- Discard dashboard pending drafts on Cancel after draining autosaves; retain the editor on discard failure (#263).

- Reject failed, malformed, or empty existing-instance config loads in the PB7/PB8 editor and keep saving blocked, including clicks made during initialization (#291).
- Wait for PBData service activation before completing database maintenance; retain recovery protection when restart verification fails (#289).
- Close DB Tools schema connections deterministically and initialize databases in WAL mode (#286).
- Return empty balance and top-symbol results without opening the database when no users are selected (#285).
- Apply selected users to dashboard template widgets and use distinct names for multi-user creation (#262).
- Confirm discarding unsaved Market Data settings before switching exchanges, preserving edits when cancelled (#255).

- Resolve order-sync symbols through CoinData mapping, preserve cached orders for unsupported symbols, and normalize inverse order symbols without fixed suffix slicing (#279).

- Authenticate dashboard sidebar navigation through the standard HttpOnly cookie/Bearer dependency instead of header/body-token-only handling (#280).

- Recover abandoned task jobs during daemon scheduling and manual Run, with a 60-second launch grace period; preserve live workers and move cancelled orphan jobs to failed (#287).

- Bound PB8 Optimize OHLCV lookup stopping to 10 seconds, restoring controls on timeout and ignoring late responses after cleanup (#292).
- Keep Backtest Add to Run flows on the current page and show an error when draft creation fails or returns no usable draft ID (#293).

- Confirm JSON config imports before replacing current PB7/PB8 run-editor values, preserving unsaved changes when cancelled or when confirmation is unavailable (#245).
- Confirm replacement of Suite scenarios while a scenario editor is open, preserving unsaved drafts when template, reset, or generated-scenario replacement is cancelled (#247).

- Close Services Monitor price-snapshot SQLite connections deterministically and hold the shared database maintenance lock while reading; return HTTP 409 during conflicting maintenance (#284).
