# v1.94.5

- Highlighted the available PB8 installation as a filled blue VPS Manager action so it no longer gets lost among routine update buttons.
- Isolated browser session cookies by visible host and port so parallel PBGui instances, including SSH-forwarded localhost ports, no longer sign each other out.
- Refreshed and consumed monitor-agent package status immediately after successful Linux updates instead of leaving the old pending-update count visible for up to an hour.
