# v1.95.24

## Added

- Added secure direct Tiingo token reveal/create/replace controls to Market Data and explicit stored third-party API-key reveal controls to the TradFi API Keys setup.

## Changed

- Updated TradFi credential regression contracts to permit an explicit authenticated single-credential reveal flow while keeping bulk and settings responses secret-free.

## Fixed

- Displayed each archived PB8 backtest's active strategy in the mixed Archive results table while leaving pure PB7 archive tables unchanged.
- Filtered focused Market Data job history by job type before applying the API result limit so retried Best 1m jobs remain visible after completion.
- Exposed the Market Data Tiingo eye handler to its inline button so clicking the eye actually requests and displays the stored token.
- Requeued non-cancelled Market Data jobs during an API-triggered worker shutdown so rate-limit waits and other resumable work no longer become false failures after a restart.
