# v2.04.10

## Fixes

- Fix AI Chat Stop during initial conversation creation; late creation responses can no longer submit the cancelled prompt (#297).
- Add authenticated PB8 Backtest scenario catalog, preview and candle-chart endpoints (#341). Preserve drawn windows and refresh the visual editor when base dates or exchanges change (#347).
- Allow TOKEN coin override objects in Optimize draft recovery while continuing to reject nested credential fields (#344).
- Prevent duplicate Market Data Status actions, disable Refresh during active runs, and report non-JSON server errors (#354). Lock Delete by Date during confirmation and deletion and expose failure feedback (#355).
- Reuse the managed PB8 helper for config preparation, loading and override validation (#359). Return instance-save responses after durable local persistence without waiting for SSH activation; show Activation pending while cluster delivery completes (#360).
- Verify Profit Sweep account changes already reset pending actions correctly; add regression coverage proving stale cancellation responses cannot unlock a newer action (#346).
- Keep Save & Queue continuously disabled with a stable progress label until completion. Show confirmed preparing jobs immediately and refresh the full queue in the background.
- Prevent duplicate loading of LogViewerPanel. Display saved PB8 reductions in the Limits table's Stat column.
- Disable local CPU controls for Vast execution and preserve their contextual dotted-label help. Explain that Vast GPU Settings controls the rental CPU minimum and Cloud Auto uses the effective rented allocation; preserve local values when switching back (#352).

## PB8 compatibility

- Review PB8 update `1f8f52b69..88e618710`; expose independent GPU rank and objective-tolerance controls with native null/zero semantics.
- Expose local optimizer offline simulations, document Backtest's existing Additional Parameters control, and respect PB8's offline policy in readiness/inception lookups. Disable remote preload in offline readiness results.
- Keep the pinned Vast worker image unchanged. Reject unsupported offline/custom drift settings before export; no offline rollout to Vast is included.
- Update English and German guides, the displayed version and the API restart serial.

## Validation

- Final release checks: 1,502 focused backend, frontend, browser, navigation and help tests passed; 2 skipped.
- Full offline suite: 10,408 passed, 48 skipped. Two pre-existing logging-policy audit failures were reproduced using unchanged HEAD files: ZIP-reader classification in `vast_performance.py` and protocol output in `setup/vast_gpu_benchmark/sync_input.py`.
- PB8 compatibility checks: 1,255 focused tests passed, 2 skipped. Direct checks against installed PB8 `88e618710` verified new setting round trips and missing-input failure without network access using an isolated empty cache.

Restart the PBGui API and reload the page to use these changes. No bot/VPS host or Vast worker was updated by this release.
