# v1.92.16

- Routed secondary-master legacy credentials through master-encrypted migration candidates so only the deterministic coordinator publishes new Cluster credentials.
- Finalized and deactivated stale direct-publish CMC records after signed acceptance, allowing secondary-master legacy sources to be cleaned safely.
