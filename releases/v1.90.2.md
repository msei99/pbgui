# v1.90.2

- Fixed master PBGui update and branch-switch playbooks so the dedicated PBMonitorAgent user service is enabled and restarted on master nodes, matching VPS updates.
- Fixed master Services UI, local systemd migration, and local/remote browser installers so PBMonitorAgent is listed, installed, enabled, and started on master nodes.
- Fixed shared log viewer panels reconnecting automatically after a transient WebSocket disconnect, such as during PBGui service restarts.
