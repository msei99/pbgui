# v1.95.2

## Fixed
- Treated a VPS reinstalled under a previously removed hostname as a new Cluster identity: setup now rotates the tombstoned node mapping, and PB8 Run targets remain unavailable until the replacement host completes an explicit Remote Join.
