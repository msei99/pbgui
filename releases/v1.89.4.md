# v1.89.4

- Fixed local Services systemd migration status so disabled default units and legacy start.sh keep migration available, and successful migration removes the legacy start.sh script.
- Made PBRun and PBData service monitoring workload-aware so disabled services are not auto-restarted on hosts where no local bots or PBData users require them.
- Defaulted new backtest parameter dialogs to the queue setting for Use PBGui Market Data.
- Debounced VPSMonitor server CPU alerts so they require 60 seconds of continuous over-threshold CPU and recovery messages show current metrics.
