# v1.87

- Implemented version-derived Config Archive paths, own-archive migration, Optimize config archive export/import, archive-result rebacktests, and liquidated archive cleanup.
- Added Archive Retest & Replace with successful-result-only archive replacement plus daily/weekly schedules ending at yesterday or using Last X days.
- Cleaned up local retest results, queue entries, logs, and snapshot configs after successful Archive Retest & Replace.
- Made local Backtest Result deletion tolerate stale rows that were already removed by background cleanup.
- Added an own-archive Remove Duplicates action with dry-run confirmation for duplicate archived Backtest results based on matching result metrics.
- Added live ArchiveSync log progress for Git Push and a separate Compact History action with estimated space savings and `--force-with-lease` protection.
- Added per-archive README configuration for static GitHub-visible archive notes ahead of generated PBGui score sections.
- Added an English README profile for the configured `mani` archive using the new static README configuration.
- Added PBGui archive result scoring with manifest persistence, README score-table generation before Git Push, and an in-app score preview/rebuild dialog.
- Changed the archive score preview to render the complete generated README Markdown in a wide resizable dialog.
- Made generated archive README score tables include the complete scored result set instead of a shortened preview slice.
- Moved the full generated archive score table into linked `SCORES.md` and sortable `SCORES.html` pages, with clickable Config names and no separate Exchange/Result columns.
- Made generated `SCORES.html` result links point to GitHub repository tree URLs so they work from GitHub Pages.
- Added a linked Top 10 score table to the generated archive README score summary.
- Rendered the generated README Top 10 score table as Markdown so PBGui's preview displays it readably.
- Updated PBGui archive scoring to version 2 with stricter drawdown and equity/balance-diff caps from 30% upward.
- Fixed README preview relative archive links so result links no longer navigate to PBGui API 404 pages.
- Restored the selected Archive Results view, archive name, and archive tab after refreshing the Backtest page.
- Added Archive Optimize sidebar actions to open archived Optimize configs directly in V7 Optimize, import/delete configs, and view configs by double-clicking rows.
- Preserved selected Archive backtest rows across automatic archive refreshes.
- Hardened Backtest Results rendering and silenced background Archive refresh failures.
- Kept own-archive actions such as Retest & Replace visible after switching back from Queue to Archive.
- Made Backtest CPU slot changes take effect while the worker is waiting for a free slot, without requiring a restart.
- Added auto-scroll while drag-selecting Backtest rows so long visible lists can be selected past the current viewport.
- Reduced Config Archive CPU load by caching archive result listings, using fast overview status, and debouncing background archive refreshes.
- Reduced Archive Results load time by avoiding expensive per-result directory fingerprints, caching archive overviews server-side, and debouncing archive-list refresh bursts.
- Added central notification logging hooks for shared PBGui navigation pages and deduplicated duplicate `/api/notify_log` writes.
- Logged remaining notice-style dialogs to persistent Notifications and removed native browser confirm/alert fallbacks from custom dialog helpers.
- Added visible filtered/total counts to the Backtest Results and Archive Results toolbars.
- Added Config Archive manifest hardening with atomic `pbgui/archive_manifest.json` rebuilds after archive mutations.
- Reduced duplicate Backtest UI load-error toast spam during API reconnects and made the restart overlay probe `/health`.
- Added a plan for version-derived Config Archive paths, Optimize settings sharing, safe legacy archive migration, archive rebacktests, and liquidated-result cleanup.
- Restored the Pareto Explorer compact gain metric when five or more scoring metrics are present.
- Reworked the Market Data OHLCV chart interaction model for smoother mouse-wheel zooming, stable panning, complete candle coverage when switching timeframes, and live price-axis updates.
