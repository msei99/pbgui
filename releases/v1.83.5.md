# v1.83.5

- VPS Manager: allowed Existing VPS probe to run with the entered IPv4 before local `/etc/hosts` is updated, requested local sudo only when saving the import needs to add or replace the host entry, and kept the import modal open with progress while saving.
- VPS Manager: kept the current VPS subview, such as Change VPS, when switching hosts with the sidebar hostname selector.
- VPS Manager: fixed systemd migration so PBRemote and PBCoinData are only enabled, started, and verified when their remote `pbgui.ini` settings are configured.
- VPS Manager: recorded local master CPU, memory, disk, and swap telemetry in the same 24h host history store used for VPS hosts and made all master telemetry meters open the correct master history.
- VPS Manager: moved systemd migration sidebar status to monitor-collected host metadata so unknown states stay neutral, running/failed tasks override stale previews, and completed migrations turn green from live monitor data.
- VPS Manager: recovered missing VPS hostnames from host JSON file names when loading legacy or corrupted VPS records.
