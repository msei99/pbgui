# v1.92

- Fixed the credential rolling-upgrade restart deadlock by allowing protocol-upgrade wait states to restart while preserving restart blocks for actual coordinator errors and mutable operations.
- Fixed the first v2 key claim for an existing unsigned legacy Cluster node so rolling credential migration can cross the protocol barrier without weakening subsequent sequence-bound key authentication.
- Made credential cleanup process-aware during rolling upgrades with an owner-only PID/start-time capability registry, lifespan-aware direct/Uvicorn API detection, normalized service-level v2 ACK metadata, automatic stale/crash handling, atomic cutoff-safe shadow creation, and no-follow legacy source reads.
- Made CMC and TradFi credential migration a zero-order rolling upgrade with local owner-only legacy shadows, all-active-v2 automatic cutover, passive outdated-node status, in-place shadow promotion, and cutoff retirement of unmatched shadows.
- Closed final credential migration gaps with retained post-cleanup sentinel scans and one constant-time cross-origin dedupe/conflict path for coordinator, forced-command, and VPS imports.
- Hardened credential migration with freeze-before-inventory barriers, outbound candidate reconciliation, exact signed membership-epoch authorization, remote scan parity, conflict resolution, and durable mutation idempotency.
- Replaced raw per-VPS CoinMarketCap key handling in VPS Manager, monitoring, and automation with secret-free Cluster credential capability, generation, and active-key metadata, including fail-closed service enablement and legacy inventory scrubbing.
- Completed the TradFi credential-vault cutover with metadata-only APIs, server-side Tiingo resolution, master-only PB7 subtree projection, process-safe exchange/TradFi merge ownership, persistent projection retries, and secret-free browser and subprocess boundaries.
- Pinned every PB7 install and standard update to the known v7.12.0 fleet commit, added fail-closed major-version guards before branch switches, and prevented upstream `master` from replacing the PB7 runtime with PB8.
- Documented the Passivbot v8 integration impact and phased rollout, including PB7 pinning, isolated PB8 runtimes and configs, controlled v7 migration, and dedicated-node guidance for small VPS hosts.
- Added an owner-only CMC and TradFi credential store, resumable cluster-wide legacy migration with signed freeze/cutoff barriers and permanent legacy write denylists, plus a fair CMC pool with authority epochs, persistent quotas, best-effort relay leases, and provider-driven failover.
- Added signed Cluster Sync v2 membership and credential operations, RFC 9180 HPKE sealed-secret relays, recipient rewrap, node-key rotation, ACK-gated activation, and master-only TradFi projection.
- Removed the API-key editor's browser URL-token fallback and broadened the regression guard against browser token query reads.
- Hardened validated AWS profiles and VPS inventory to owner-only permissions, and moved VPS host, master, async patch, compare-and-swap pending-state, version-aware merge, and delete operations onto shared locked atomic persistence.
- Added an exact startup migration for retired PBRemote and Streamlit-era INI sections while retaining PB6 compatibility sections.
- Persisted V7 Backtest sorting independently for Configs, Results, Archive Results, and Legacy Results, with validated fallback when saved columns become unknown.
- Completed Strategy Explorer edge parity across PBGui Simulation and PB7 Engine modes with fee-override semantics, realized-loss orchestrator state, rolling net-PnL lookback, and a real-result differential confirming unchanged default results plus PB7-matching override behavior.
- Added authenticated live-session resource diagnostics, periodic SSE session revalidation, cancellation-safe disconnect cleanup, shared credential snapshots, and final client-cycle collection; production validation confirmed clean 1-user and 10-user teardown, stable PBData memory, bounded FastAPI warm-cache memory, and unsupported-exchange DB fallback.
- Bounded PBData shutdown steps so cancellation-resistant exchange WebSocket clients cannot force systemd to kill the service after its stop timeout.
- Completed the INI modernization with canonical snapshots, transaction-safe writers, targeted last-known-good reloads, apply timing UX, restart signaling, and offline regression coverage.
- Completed the local `pbgui.ini` reader audit with canonical snapshots, exact apply metadata, finite monitor-threshold validation, batched alert persistence, and cwd-relative reader policy coverage.
- Added explicit INI apply-timing metadata, runtime API restart reasons, additive settings API feedback, and safe Services/Logging GUI save messages.
- Consolidated PBCoinData live settings into one validated last-known-good snapshot with daemon-owned change wakeups and bounded malformed-generation retries.
- Consolidated PBData live settings into one validated last-known-good snapshot model with a single shutdown-safe watcher and deterministic poller interval reloads.
- Added canonical read-only INI snapshots and locked atomic batch transactions with concurrency and rollback coverage.
- Migrated PBGui-controlled local INI writers and related API settings saves to shared locked batch transactions with policy and concurrency coverage.
- Canonicalized PBGui-owned job, VPS Manager, monitor-agent, preload, backtest, optimize, Pareto, and API handoff transcripts under `data/logs` with managed, lock-safe rotation and targeted legacy readers.
- Added process-safe startup migrations for obsolete logging artifacts and configurable managed log-family rotation scopes.
- Extended VPS cleanup to remove exact legacy PBRemote and sync log generations while preserving unrelated synchronization logs.
- Fixed the shared API restart action to authenticate cookie-only browser pages without exposing or requiring a JavaScript session token.
- Added a follow-up startup migration for retired `PBRemote`, `PBMon`, and `sync` logs and removed those obsolete files from rotation settings.
- Audited every rotation entry, retired stale `FastAPI`, `FileSync`, `PBStat`, `V7ConfigSync`, and `config_archives` logs, and grouped Auth and Live Session helpers into `PBGui.log`.
- Validated full-history KuCoin 1m archive quality across 20 liquidity-ranked markets and defined native-coverage eligibility thresholds for automatic backtest data.
- Classified KuCoin and Gate.io as unsuitable historical OHLCV sources after archive-density and retention validation, and removed their integration from the active roadmap.
- Migrated remaining Phase 3 runtime log writers to centralized redacted logging, classified Tier-3 ownership, and retained dedicated transcripts and pipeline logs.
- Added request-scoped logging context and operational metadata for database and bot lifecycle events.
- Added logging policy guardrails and hardened the standalone remote VPS monitor logger with redaction, locking, and bounded rotation.
- Completed the Logging migration with canonical paths, centralized credential redaction, concurrent-safe rotation and purge, cookie-only monitoring, operational request context, explicit tier ownership, and English/German documentation.
- Hardened INI watching and VPSMonitor live reload with atomic generation detection, async wakeups, validated last-known-good snapshots, and owned shutdown-safe reload tasks.
