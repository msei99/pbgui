# v2.01.9

## Fixed

- Preserve distinct PB8 Pareto validation dates through the Backtest queue handoff and add an explicit Training + Holdout + Full timerange mode for direct result comparison.
- Wake Profit Sweep after DB Sync imports new local income-history rows so remotely scanned accounts do not have to wait for the periodic fallback.
- Persist approved Python analysis output in PBGui AI conversations before optional model follow-up, pass bounded result context to the summary turn, and keep internal continuations outside action-request enforcement (#133).
- Synchronize Sweep Cycles Starting balance from the active PB8 base config whenever Recalculate is clicked (#130).
- Add Full timerange-only validation for standard Pareto results and let combined validation continue with Full timerange when Holdout dates are unavailable (#132).
- Restore the selected Optimize result after refreshing `#paretos` by revalidating a versioned session result ID without storing absolute paths (#131).
